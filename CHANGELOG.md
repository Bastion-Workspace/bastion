# Changelog

All notable changes to Bastion AI Workspace will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.55.0] - 2026-03-28
- Update: Bump version to 0.55.0 across application and Docker
- Refactor: Post-purge dead code cleanup — removed unused orchestrator utils/models/services, `project_content_tools`, `response_sanitizer` extraction from deleted writing subgraph utilities; dropped unused `Route.subgraphs` / `Route.context_loader` and stub `routes/loaders`
- Refactor: Removed dead `general_project_nodes` package and `general_project_models.py` (no `GeneralProjectAgent`; editor `general_project` route remains Agent Factory-only)
- Refactor: Removed dead `ElectronicsAgent`, `electronics_nodes`, diagramming subgraph, and `electronics_models` / `diagramming_models` exports (nothing dispatched to them; editor `electronics` route remains Agent Factory); `apply_operations_directly` allowlist no longer references `electronics_agent`
- Breaking: Removed built-in `FullResearchAgent`, `escalate_to_research`, and research-only LangGraph subgraphs; default built-in playbook uses document/web tools directly; migration `113_builtin_playbook_remove_full_research_agent.sql` updates playbook and seeds optional deep-research template
- Refactor: Removed orphaned `FictionEditingAgent`, editor/writing LangGraph subgraphs (outline, rules, style, character, article, podcast, proofreading, fiction flat graph, proposal subgraph package), and `fiction_utilities.py`
- Breaking: Orchestrator routing — removed ConversationalEngine, ResearchEngine, ChatAgent, WritingAssistantAgent, and ProposalGenerationAgent; all skill routes dispatch via CustomAgentRunner (default or built-in profile); checkpoint continuity preload uses CustomAgentRunner; `EngineType` is custom_agent only
- Fix: Custom agent image search and generation — persist structured `metadata.images` (URL-based only) so chat gallery survives refetch; strip data-URI screenshots before DB save; no duplicate inline markdown images (gallery only)
- Fix: Chat sidebar message list respects active branch path after edit-and-resend (refetch no longer keeps the old full thread when streaming or tree walk edge cases)
- Feature: Chat edit-and-resend with branch navigation — message tree (`parent_message_id`), per-branch LangGraph checkpoints, API branch/switch endpoints, and fork navigator in the UI
- Fix: PDF export headings rendered at body size due to missing space in heading ID injection (`<h1id="...">` → `<h1 id="...">`)
- Fix: PDF export — watermark no longer emits a second `body {}` rule; `position: relative` merges into typography `body`; headings use `white-space: normal` and `overflow-wrap: break-word`; watermark stacking `isolation: isolate`
- Feature: ElevenLabs TTS — optional synthesis `model_id` (e.g. Flash) via gRPC, voice settings (BYOK + admin server), and allowlisted REST resolution
- Feature: PDF export — typeface style Mixed / Serif / Sans; stronger heading CSS for `.main-body` and `.message-content`; normalize ATX `#` headings for markdown sources (blank lines + column 0)
- Fix: PDF export — native Markdown headings for PDF (removed placeholder/restore); normalize U+FF03 fullwidth `#` before parse; repair `<p># Title</p>` to real `<h1>`–`<h6>` when needed; heading CSS includes `display: block !important` for WeasyPrint
- Fix: PDF export — Org-to-Markdown headings at column 0 with surrounding blank lines; PDF pipeline uses markdown without nl2br so headings parse as h1–h6 and typography CSS applies
- Fix: ElevenLabs TTS — map OGG/Opus requests to `opus_48000_128` (API removed `ogg_44100_128`, which caused 403 invalid_output_format)
- Feature: TTS latency — ElevenLabs `convert_as_stream` (MP3), Piper ffmpeg OGG streaming, OpenAI/ElevenLabs client reuse; shared `VoiceAvailabilityProvider` so document read-aloud uses server TTS; MediaSource MP3 + OGG; sentence chunking for long text; no per-speak voice-settings fetch
- Feature: TTS — richer markdown/org sanitization for speech; `AbortController` on synthesize/stream fetches so stop cancels in-flight requests; DocumentViewer “Export as Audio…” (browser MP3 merge for short docs, Celery job + download for long docs); `POST/GET /api/audio-export` routes
- Fix: MCP client — 60s timeout on `initialize` plus one operation (discover/call); phase logging for stdio/SSE/streamable HTTP to diagnose hangs
- Fix: MCP discover_tools handles tools/list entries as dicts (not only objects) so Blender MCP and similar servers populate discovered_tools
- Fix: MCP server Test persists discovered_tools like Discover so the Settings list shows tool names after a successful test
- Feature: MCP servers in Settings — environment variables (JSON) and Edit dialog
- Feature: MCP Discover/Test REST calls run via tools-service gRPC (`DiscoverMcpServer`) so UI uses the same runtime as agent execution (e.g. stdio `uvx`/`npx`)
- Feature: Runtime MCP server hosting via uvx/npx in tools-service (no image rebuild needed for new MCP servers); `host.docker.internal` and persistent uv/npm caches for stdio MCP subprocesses
- Fix: Piper TTS — treat Piper output as raw PCM (`pcm` / s16le) with `voice.config.sample_rate` when converting to WAV/MP3/OGG (was mislabeled as WAV for ffmpeg, causing `invalid start code` / decode errors)
- Fix: Piper TTS — newer piper-tts yields `AudioChunk` from `synthesize_stream_raw`; convert via `audio_int16_bytes` before joining PCM (fixes gRPC `expected a bytes-like object, AudioChunk found` and silent browser fallback)
- Fix: Voice settings — per-backend TTS voice memory (server default vs Piper vs BYOK cloud vs BYOK Piper); switching speech backend no longer clears saved voices; cloud voice cleared only when changing to a different saved cloud provider
- Feature: PDF export — Article vs Book layout (`pdf_layout`), book margins and page-number placement, optional per-section overrides via `POST /api/export/pdf/heading-outline` and DocumentViewer dialog
- Feature: voice-service downloads default Piper voice `en_US-arctic-medium` from Hugging Face on first start (persists on `voice-models` volume); `PIPER_BOOTSTRAP` to disable; default `PIPER_VOICE` aligned to arctic-medium
- Feature: User Home dashboards — PostgreSQL `user_home_dashboards` (migration `111_user_home_dashboards.sql`); migrate from `user_settings` `home_dashboards_v2` / `home_dashboard_v1` on first access; same REST routes (`/api/home-dashboards`, legacy `/api/home-dashboard`), `/home` / `/home/:dashboardId`
- Update: Added dev-note `docs/dev-notes/USER_DESIGNABLE_DASHBOARD_CONCEPT.md` for user-composable dashboard pages and primitives (concept; disambiguates existing Operations / Line dashboards)
- Feature: Appearance — theme mode Light / Dark / System (follows OS); user menu and mobile drawer hide the quick light/dark item while System is selected; no separate Appearance menu row (use Settings); preference stored as `themePreference` with legacy `darkMode` localStorage migrated
- Fix: BYOK TTS synthesis — `user_byok_tts_engine` (Piper/Browser) is applied before BYOK credential context so Piper works even when a cloud provider row is still linked; voice-settings mutation updates React Query cache from PUT response so chips stay selected
- Fix: BYOK TTS — Piper and Browser chips always visible (not hidden behind providers query loading); cloud chips and add-provider form load separately
- Fix: `GET /api/voice/voices` honors `?provider=piper` when the user has BYOK cloud TTS linked (no longer overwrites with the linked provider); Piper chip and voice picker stay in sync; empty Piper model path surfaces a warning in Settings
- Fix: TTS stop no longer falls through to browser speech (session invalidation + detach audio handlers); BYOK voice-settings expose `effective_server_provider` so localStorage `piper` does not override ElevenLabs; `serverAvailable` seeds from voice status cache; stop buttons dispatch only the shared stop event
- Feature: TTS streaming — `SynthesizeStream` gRPC, `POST /api/voice/synthesize/stream`, ElevenLabs/OpenAI chunk providers, frontend MediaSource (OGG/Opus) with buffered MP3 fallback
- Feature: Admin LLM catalog slice cached in `AdminProviderRegistry` (invalidated on enabled-model save); partial-catalog guard; role-setting orphans (`orphaned_role_models`); Celery beat task every 6h notifies admins via WebSocket; `POST /api/models/catalog-slice/invalidate`; Settings UI for stale role models (jump/clear)
- Feature: LLM catalog hygiene — `/api/models/enabled` returns orphaned IDs vs live catalog, `selectable_chat_models`, and chat/external-chat use catalog-aware lists; admin Settings warns and removes stale enabled IDs; BYOK provider card can prune stale toggles; `POST /api/models/select` rejects unavailable models
- Update: Models settings — “Current AI Model Configuration” and its API calls are admin-only; clarify copy (global defaults vs sidebar/prefs); drop unused `/api/models/current` fetch from status widget
- Fix: My AI Providers — `Use my own API keys` switch aligned to BYOK (`use_admin_models`), static label; hide redundant “Available AI Models” card for non-admins when using own providers
- Fix: Voice settings — BYOK toggles for TTS/STT show on when user keys are active (`checked` aligned to “use my API keys”, not `use_admin_*`)
- Fix: `useTTS` — refresh voice settings immediately before server synthesize so editor/chat read-aloud uses account TTS (not stale React state or localStorage from before prefs load)
- Fix: `useTTS` server playback — session token ignores stale synthesize/Audio after rapid read-aloud clicks so only one stream plays
- Feature: Voice — built-in TTS chips (server default / Piper / Browser only); BYOK adds cloud-provider chips per saved key; `user_byok_tts_engine` and `prefer_browser_tts`; read-aloud uses browser-only path when Browser TTS is selected
- Feature: Voice selection — admin/built-in TTS voice and provider in Settings (`user_admin_tts_*`), synthesis fallback in `voice_api`, `effective_server_*` in voice-settings for `useTTS` (server prefs before localStorage), `bastion-voice-settings-changed` event
- Feature: User voice BYOK — per-user TTS/STT API keys (ElevenLabs, OpenAI, Deepgram), Settings Models tab UI, voice-service gRPC credential overrides, migration `110_add_user_voice_providers`
- Feature: Markdown PDF export — optional table of contents (depth H1–H6), footer page numbers, optional text watermark (all or first page), and page breaks before selected heading levels; dialog controls in DocumentViewer
- Feature: Markdown editor — Tools menu (Frontmatter + Insert/Edit table) and graphical table drawer with sort, column/row reorder, alignment; shortcut Mod+Shift+T
- Feature: Markdown editor — drag left edge to resize Frontmatter and Table drawers; width persisted in localStorage
- Feature: Agent Factory playbooks — optional per-step `user_facts_policy` (`inherit`, `no_write`, `isolated`) to restrict remembered-fact injection and save/read tools on conditional branches; UI in step config drawer
- Fix: Prevent edit proposals from leaking across documents during tab switches (AbortController + stale-response guard + `proposal.document_id` check in `DocumentViewer.syncPendingProposalsFromDb` / `handleEditProposal`; optional `signal` on `getPendingProposals`)
- Fix: Pending document edit proposals — `list_pending_proposals_for_document` drops older-proposal ops whose resolved range overlaps a newer proposal (no DB change); dev-only overlap warning in `documentDiffStore.mergeProposalDiff`
- Fix: Live editor proposed edits — CodeMirror `Decoration.set(decos, true)` so diff highlights render when ranges share positions (was throwing unsorted-range errors); same for inline edit suggestions
- Feature: Data workspaces — Apache Arrow IPC over gRPC for SQL query results and paginated table data; PyArrow CSV/JSON import with batched `to_pylist` inserts; 50 MiB gRPC message limits on data-service and backend client (`pyarrow>=15`)
- Feature: Data workspaces (Arrow phase 2) — REST avoids double JSON on query results; tools-service passes Arrow IPC to orchestrator (`QueryDataWorkspaceResponse.arrow_results`); native table bulk import uses PostgreSQL COPY; typed Arrow for native `GetTableData`; streaming `ExportTableData` (csv/parquet/arrow_ipc) and `GET /api/data/tables/{id}/export`
- Feature: RSS agents — `ListStarredRSSArticles` gRPC and `list_starred_rss_articles` tool to list all starred articles across feeds (paginated); tools-service `get_starred_articles` uses feed join for `feed_name`
- Feature: Agent RSS gRPC/tools — full article metadata (read/star/import, feed_id/name, created_at), unread/starred filters on list/search, per-feed unread_count in list feeds, MarkArticleUnread/SetArticleStarred RPCs and tools; tools-service `search_articles` with feed join (fixes SearchRSSFeeds)
- Update: RSS article viewer — remove full-content source chip; mark article read when expanding (“Read more”)
- Fix: RSS article viewer — Unread Only list keeps an article visible while it stays expanded after mark-read (removed when collapsed)
- Fix: RSS article viewer — after Read less removes a read item from Unread Only, scroll the next article into view smoothly
- Fix: RSS article viewer and RSS feed settings table show published / last-check times with user timezone and 12h/24h settings (`formatInstantDateTime`, shared `userTimezone` / `userTimeFormat` queries)
- Fix: RSS "Delete all read" skips starred articles (server + UI list filter); confirm dialog notes starred are kept
- Fix: RSS article HTML sanitizer — treat any `data:` `img` `src` as lazy placeholder (resolve `data-src` etc.); rescue `<img>` from `<noscript>` before removal; stop stripping `webfeedsFeaturedVisual` / `attachment-post`; INFO log image count before/after sanitization
- Fix: GReader API — INFO logging for `/reader/api/0/token`, `/edit-tag`, `/mark-all-as-read`; catch-all POST logs unmatched paths under `/api/greader/`; GReader stream item body no longer applies `html.unescape` after ingest
- Fix: GReader `stream/items/contents` includes `id` and `updated` (NetNewsWire Codable); `stream/items/ids` returns `continuation` for pagination
- Fix: GReader API — honor include-target query param `it` (e.g. `it=user/-/state/com.google/read` on reading-list) for `stream/items/ids` and `stream/contents`; FreshRSS-mode Read You used this for read-id sync and previously received all items
- Update: PDF export — slightly smaller default typography; optional landscape orientation (`page_orientation` API, document tab export dialog)
- Fix: Markdown document PDF export — merge pipe-table rows split across lines before parsing so tables render as HTML; table cell wrapping CSS for WeasyPrint
- Fix: GReader API — `s=user/-/state/com.google/read` returns only `is_read=true` rows (`include_only_read`); returning all articles for the read stream caused clients (e.g. Read You) to mark every fetched id as read and empty the unread list
- Fix: GReader API — `stream/items/ids` itemRefs now include `timestampUsec` and `directStreamIds` (per Google Reader API spec); item `summary`/`content` and stream responses include `direction: ltr` for mobile client compatibility
- Fix: GReader unread-count — treat `is_read` NULL as unread (match stream pagination); include `user/-/state/com.google/reading-list` aggregate in `unreadcounts` for clients that ignore per-feed rows
- Feature: After creating a user or global RSS feed, enqueue an immediate `poll_rss_feeds_task` (`force_poll`) so the first articles arrive without a manual refresh
- Fix: RSS feed “last updated” in file tree uses user timezone and 12h/24h from settings (`formatInstantDateTime`, shared `userTimezone` / `userTimeFormat` queries)
- Fix: RSS feed rows in file tree show last poll time using API field `last_check` (was reading nonexistent `last_updated`, always “Never”)
- Feature: RSS file tree context menu — Refresh All Feeds on virtual RSS folders; Refresh Feeds and Mark All As Read on category rows (with confirm); Mark All As Read on individual feeds
- Fix: RSS sidebar and Settings unread chips refresh when articles are marked read, mark-all-read, deleted, or all read articles deleted (`rss-unread-counts-updated` + React Query invalidation)
- Fix: GReader `subscription/list` and `tag/list` — category labels use the same title-case rules as the web RSS sidebar; label stream ids use a lowercase canonical segment so mixed DB casing maps to one folder
- Fix: RSS article content — prefer `content:encoded` / `entry.content` when substantial; stricter truncation detection; crawl path no longer falls back to raw full-page HTML or merges Crawl4AI images into body; sanitizer strips more ad/chrome buckets and ad/tracking image URLs; GReader item body prefers `full_content_html`; RSS viewer sanitizes HTML with DOMPurify before render
- Fix: GReader API — do not filter streams by `ot` against article timestamps (clients often send stale watermarks, hiding newer items); `stream/items/ids` accepts continuation `c`; subscriptions and tag list include `Uncategorized` when feed `category` is empty
- Fix: Celery workers run shared `ensure_user_memory_schema_columns` on process init so `conversations.needs_session_summary` / `user_episodes.is_aged` exist without relying on API lifespan (fixes `detect_idle_sessions_task` on DBs missing migration 108)
- Feature: RSS starred articles, bulk mark-read/delete-read endpoints, and optional Google Reader API (`/api/greader/`, `GREADER_API_ENABLED`); migration `109_rss_starred_and_greader.sql`
- Refactor: Session-level user memory — combined post-session LLM analysis (facts + one episode) replaces per-turn episode and per-N-message fact extraction; `conversations.needs_session_summary`, idle beat + delete/new-chat triggers; unified `user_memory` gRPC metadata; tiered episode retention (48h `is_aged`, 7d graduate-facts-then-delete); migration `108_memory_session_summary.sql`
- Feature: Agent line chat dispatch — `@line-handle` routes to line CEO with heartbeat-style briefing, sticky `active_line_id` until `@auto`, sub-agent `send_to_agent(wait)` replies appended to chat via gRPC `AppendLineAgentChatMessage` and agent-status WebSocket; line context chip in chat UI
- Update: `list_rss_feeds` tool — `formatted` output grouped by category (Documents RSS folders), per-feed article_count, scope, last_polled, truncated URL; registry description clarifies digest workflow
- Feature: Agent profile summarization settings — DB columns `summary_threshold_tokens` / `summary_keep_messages`, REST + gRPC `GetAgentProfile`, Agent Factory Identity UI (with chat history), debounced save in Agent Editor
- Update: Org TODOs view — segmented status (Active/Upcoming/Done/All), Filters popover (tag, priority, category + hints), compact Sort menu (Organization vs Task groups), removable refine chips; row state dropdown remains body2 sentence-case labels
- Fix: RSS reader — preserve images and block structure in stored HTML (sanitized), keep paragraph breaks in plain text, render feed HTML summaries when expanded; typography styles for `.rss-article-content`
- Fix: RSS full-content images — use Crawl4AI `html` (DOM) for Readability/article HTML instead of markdown `full_content`; Crawl4AI `images` kept as article metadata only (not merged into body HTML); resolve `//` URLs and common `data-src` lazy attributes in sanitizer
- Update: RSS UI — remove manual “Extract Full Content” bulk action and `/api/rss/articles/extract-full-content`; add Celery Beat job `rss-full-content-backfill` (every 6h, up to 10 articles with `full_content` null older than 2h) to retry failed extractions
- Feature: Agent Factory middleware — `SummarizationNode` wired after `prepare_context` in `CustomAgentRunner` when `prompt_history_enabled` (profile keys `summary_threshold_tokens`, `summary_keep_messages`); `StructuredOutputParser` centralizes JSON extraction for pipeline `llm_task` / `llm_agent` steps with `output_schema` / `json_output`
- Feature: Unified `MessagePreprocessor` middleware — single pipeline (tool-prefix strip, data-URI filter, optional ManuscriptEdit sanitization) for BaseAgent, chat/electronics/fiction callers, custom agent `{history}`, and editing subgraphs; `ActionContract.retriable` + `ToolRetryNode` with transient exceptions wired into pipeline `llm_agent` tool paths; non-retriable defaults for send/reply email, notifications, file/document creates, patches, and `save_user_fact`
- Fix: Tool call summary no longer injected into assistant message content — carried via gRPC/metadata (`additional_kwargs`) so LLM responses do not echo `[Tool actions: ...]`; defensive strip on custom/chat output and before persisting streamed assistant messages
- Feature: Harden chat history pipeline — `sequence_number`/`created_at` on gRPC messages, sequence-based checkpoint merge with content fallback, stronger external-connector dedupe, `chat_history_enabled=False` sends no history, slimmer `most_recent` DB path
- Update: Custom agent `{history}` prompt — no per-message or total-char truncation; lookback alone controls included messages
- Fix: Chat history for orchestrator — persist `agent_profile_id` in conversation metadata (sticky lookback), load newest N messages for context (not oldest), default context window 20; `@auto` clears stored profile via `clear_agent_profile_id`
- Feature: Data connectors — REST auth types `basic` (HTTP Basic) and `jwt` (HS256/384/512, compound key, configurable claims/header prefix); multipart file upload (`body_mode: multipart_file`) for endpoints like Ghost image upload; Ghost Admin API template; PyJWT in connections-service
- Feature: Data connectors — SFTP, S3-compatible, and WebDAV executor types in connections-service (`execute_connector` dispatch); templates and `connector_type` merge from DB in `execute_connector_endpoint` client
- Fix: GetDocumentContent for PDFs — unpack four-tuple from `DocumentProcessor._process_pdf` (was `ValueError: too many values to unpack`)
- Feature: Stale LLM model handling — soft-retarget with WebSocket `agent_notification` (`model_configuration`), deduped user notices, auto-persist for chat/conversation/user KV and agent profile preferences; playbook save/import/clone/restore remediates invalid `model_override` steps and notifies once per playbook
- Feature: All TODOs tab — “New TODO” opens Quick Capture; Quick Capture defaults to TODO template (first in list) with advanced fields shown; global `openQuickCapture` event matches Ctrl+Shift+C
- Feature: Documents RSS sidebar — feeds grouped under category folders (themed category rows; chevron expands feeds; category row opens merged articles tab); virtual **RSS Feeds** / **Global RSS Feeds** rows open a merged articles tab across all feeds in that scope; unread counts treat NULL `is_read` as unread (SQL) and article list requests honor `limit` query; RSS tree node only when user/global has feeds; no Virtual chip on RSS nodes; folder tree refetches when feeds are added or removed
- Feature: RSS article import — default destination configurable in Settings → RSS Feeds (My Documents folder; admins may pick Global); optional per-request `target_folder_id`; imported RSS files use the same on-disk layout as other documents in the target folder
- Update: Agenda view — Microsoft 365 and org agenda times use Settings time format (12h/24h) and user timezone (same queries as status bar); org wall times are interpreted in that IANA zone when set
- Refactor: RSS infrastructure — `RSSPollingService` for Celery polling; gRPC `MarkArticleRead` / `GetUnreadCounts` / `ToggleFeedActive` and orchestrator rss tool pack; Settings RSS Feeds tab; seeded RSS Manager Agent Factory profile (migration 106)
- Feature: Org Tags Browser — tag index with counts, multi-tag filter (match any / all), optional _archive.org scan, open headings from results
- Refactor: Remove legacy execution scaffolding — dead capability workflow/intelligence modules, unmounted LangGraph native streaming API/HITL handler, unused `orchestrator_utils` helpers, Celery `orchestrator_tasks`, stub orchestrator gRPC RPCs (`StartTask`, `GetTaskStatus`, `ApprovePermission`, `GetPendingPermissions`) and related proto messages; trim `grpc_service` dead cancellation helper
- Fix: MCP `streamable_http` client — unpack `(read, write, get_session_id)` from `streamablehttp_client` (mcp SDK yield shape; fixes discover/test ValueError)
- Fix: Backend Docker image — generate `voice_service.proto` in `grpc_tools.protoc` step so `/api/voice/*` can import `voice_service_pb2`
- Fix: Celery worker — add `PyPDF2` and `pypdf` to `requirements-celery-worker.txt` so service container / document chunk backfill can initialize (matches main `requirements.txt`)
- Fix: User MCP discovery/execute — rename in-repo package `backend/mcp` to `backend/document_mcp` so `import mcp` resolves to the PyPI MCP SDK (was shadowed when backend is deployed at `/app`, causing false "mcp package is not installed")
- Fix: MCP servers API — JSON responses encode `created_at`/`updated_at`/`last_discovery_at` via `jsonable_encoder` (fixes 500 on POST/GET list after create)
- Feature: Unified external tool packs with MCP — `mcp_servers` CRUD and discovery, Tools Service `ExecuteMcpTool`/`GetMcpServerTools`, playbook-step external pack UI (email/calendar/MCP), actions and tool-packs API without service bindings; migration script `scripts/migrate_service_bindings_to_tool_packs.py`
- Feature: Agent Lines integrated into Agent Factory — Lines sidebar section, `/agent-factory/line/:id` editor with internal tabs and resizable settings/goals/task drawers; legacy `/agent-factory/lines` URLs redirect
- Update: Agent Factory architecture dev-note expanded (accurate engine paths, step types, connector gRPC path, HITL interrupts, schema, WebSocket schedule_paused shape); playbook validation accepts `browser_authenticate` step type
- Fix: Agent Factory step drawer — prompt fields use isolated debounced editors so typing does not re-render the full drawer every keystroke; save merges latest draft from refs
- Fix: Media page — Audiobooks author drill-in reads `albums` from `GET /api/music/albums/artist/...` (was treating the whole JSON body as an array, so “Books” always empty); podcast episode list duration uses more Audiobookshelf shapes (time strings, nested `audioTrack` meta, bitrate/size estimate)
- Fix: Audiobookshelf podcast playback — stream URL uses `audioTrack.contentUrl` or `/s/item/{libraryItemId}/{filename}` (not episode id in path); episode duration falls back to `audioFile` / `audioTrack`
- Fix: Music stream proxy — mirror upstream HTTP status and `Content-Range`/`Content-Length` (avoid invalid 206 responses that break HTML5 audio); frontend stream URL uses `VITE_API_URL` like other API calls and sets `crossOrigin` only for cross-origin streams; Media page track table keys deduped by index
- Update: Media page — Music / Audiobooks / Podcasts category bar uses `background.paper`, 44px height, and tab strip styling aligned with Documents / chat header
- Update: Agent Factory — tab labels use `text.primary` with `primary` icons and bottom accent stripe (matches Documents tabs); sidebar section headers use the same type icons (Agents/Playbooks/Skills/Data Connections)
- Update: Agent Factory — tab bar uses `background.paper`, fixed 44px height and borders to align with the AI chat header; empty state title row is single-line to match
- Update: Documents + AI chat shell — single `SplitResizeHandle` styling, removed 6px main gutter when chat is open; `--app-nav-height` measured from `Navigation` AppBar for fixed chat `top`/heights; chat + Documents headers match 44px tab bar (`boxSizing`, `px: 2`, no extra vertical padding)
- Feature: Default chat agent — optional non-built-in profile via `default_chat_agent_profile_id` (Agent Factory “Set as default for chat”); `grpc_context_gatherer` uses it before factory built-in; built-in profile is read-only (use reset-defaults); settings API `GET/POST /api/settings/default-chat-agent-profile`
- Feature: Image generation — auto-promote generated images to user documents with metadata, optional `folder_id` on gRPC/tool, `document_id` on `GeneratedImage` proto, conversation cleanup matches `/api/images/` URLs, daily Celery task for orphaned `gen_*` files in `web_sources/images`
- Fix: Main shell scrollbar — reverted `bastion-main-scroll` / `scrollbar-width: thin` / flex child max-width constraints (broke hover and made bars too narrow); WebKit back to 8px + `border-radius: 4px`; chat collapsed `pr: 10px`, collapsed Open chat `calc(22px + safe-area)`.
- Fix: API client — treat HTTP 204/205 (no body) as success without calling `response.json()` so DELETE flows (e.g. Agent Factory line delete) resolve and navigation runs
- Fix: Scrollbar thumb drag when chat closed — fixed “Open Chat” control (`right: ~24px`) sat above the native scrollbar strip (z-index 1300); wheel still scrolled content; inset to `right: max(56px, …)` on desktop and smaller button so thumb drags are not intercepted
- Fix: Scrollbar dragging when chat closed — inner Box `pr` was 0 when collapsed, putting the Container scrollbar flush against the outer `overflow:hidden` clip boundary (breaks hit-testing on Linux/Windows classic scrollbars); now always `pr: 2px` on desktop
- Fix: Task board compact mode — skip DragDropContext entirely when `compact` prop is set; RBD's global mousedown listener was showing "not-allowed" cursor on native scrollbar drags; drag-to-reorder remains available in the full task board view
- Fix: Collapsed chat toggle — restored to right-center (original position); DragDropContext was the scrollbar culprit, not the button; wrapper uses `pointer-events: none` so clicks to the scrollbar strip pass through
- Fix: Main column scrollbar — avoid `overflow: hidden` on the flex wrapper around `Container` (breaks scrollbar hit-testing with motion + nested overflow); small `pr` when desktop chat is open so the fixed chat panel cannot overlap the scrollbar strip; removed experimental `scrollbar-gutter: stable` on full-width `Container`
- Fix: Task board (react-beautiful-dnd) — drag handle only on the grip icon; whole-card handle caused invalid-drag cursor when using the page scrollbar; full-width shell uses `maxWidth: 100%` (not `100vw`) to avoid horizontal overflow
- Fix: Agent line dashboard — scroll the main `Container` for full-width routes instead of a nested `overflow: auto` region so native scrollbars receive pointer events (nested overflow + motion had made the bar non-interactive)
- Fix: Collapsed chat toggle — bottom-left (above status bar) so it does not cover the native vertical scrollbar or the Documents file-tree control
- Fix: Vite config — block comments must not contain `*/` (e.g. in glob `**/.js`); use line comments so `frontend-dev` loads `vite.config.mjs`
- Fix: Org editor frontmatter — declare `js-yaml` and use ESM import (replaces `require`) for Vite/browser bundles
- Fix: Vite dev — app entry renamed to `src/index.jsx` so import-analysis accepts JSX (HTML script updated); other `src/**/*.js` components unchanged
- Fix: Vite dev — `vite-jsx-in-src-js` `load` hook strips Vite’s `/@fs/` prefix before reading files so JSX-in-`.js` under `src/` is pre-transformed (failed reads no longer fall back to raw JSX for import-analysis)
- Breaking: Docker Compose — production `frontend` uses profile `prod`; set `COMPOSE_PROFILES=prod` in `.env` (see `.env.example`) for `docker compose up` to include the nginx UI, or use `docker compose --profile prod up`. Vite dev UI: `docker compose --profile dev up --build` (comment out `COMPOSE_PROFILES` or avoid `prod` in it so only `dev` runs; otherwise port 3051 conflicts)
- Feature: Frontend migrated from Create React App to Vite; production nginx build unchanged in behavior; `frontend-dev` runs the Vite dev server on host port 3051 with HMR; env vars renamed from `REACT_APP_*` to `VITE_*` (build args on `frontend` image embed client config)
- Feature: Agent line references — `document_id` in injected blocks, `{line_ref_ids}` JSON for loaded docs, `{line_ref_skipped_count}` when loads fail; team references may be read/write (global remains read-only)
- Feature: Agent lines — Data Workspace binding (additive with profile), per-workspace read vs read/write enforced via `read_only` on workspace queries; line settings UI; `data_workspace_config` on `agent_lines`
- Feature: Agent lines — configurable reference folders/files (My/Global/Team) injected as `{line_refs}` in custom agent playbooks; line settings UI; `ListFolderDocuments` gRPC for orchestrator loading
- Feature: Playbook editor — copy contiguous top-level steps to clipboard (JSON + session storage) and paste into the same or another playbook with output_key collision remapping, safe `{step.field}` rewriting when references target upstream steps, and optional confirmation when references need review
- Fix: Chat sidebar — “New Messages” control sits over the message scroll area (not the input row), hides as soon as you scroll within threshold of the bottom, and stays aligned via ResizeObserver when content height changes
- Fix: Document edit proposals — inline diff shows full proposed additions (no “show more” collapse); accepting one operation applies only that hunk and keeps other pending ops (backend trims stored operations; frontend passes operation index and re-syncs); partial-apply DB update uses JSON string for `jsonb` (asyncpg)
- Fix: Chat streaming — `active_editor` uses transport checks only (open editable .md/.org, non-empty content) when editor preference is not ignore; removed client `frontmatter.type` allow-list; no longer clears `editor_ctx_cache` on slow editor refresh timeout
- Feature: PDF export overhaul — all PDF exports (markdown documents, chat messages, conversations) now use server-side HTML-to-PDF generation via WeasyPrint for proper Unicode support, selectable text, correct typography, and robust layout. Replaces fragile client-side jsPDF/html2canvas implementations with authenticated backend API matching EPUB export pattern
- Fix: PDF export — WeasyPrint upgraded to 65.1 for compatibility with pydyf 0.11+ (fixes `AttributeError: 'super' object has no attribute 'transform'` on `/api/export/pdf`)
- Fix: PDF export — standalone Unicode box-drawing separator lines (e.g. `═`) are normalized to Markdown `---` before render so they become `<hr>` and stay within margins (not unbreakable overflow)
- Fix: Agent Line runs skip LLM-based conversation title generation (uses admin OpenRouter key); pipeline model overrides and tools (delegate_goal_to_tasks, text transforms, route selection) use user OpenRouter credentials from metadata when provided; playbook model override dropdown filters to user-sourced models when user has configured LLM providers
- Update: Agent line dashboard merges **Pause autonomous** and **Emergency stop** into **Stop autonomous** (pauses line, disables scheduling, revokes CEO run); **Activate autonomous** when active and scheduling off; background worker dispatches no-op when line is `paused`
- Update: Line settings heartbeat save aligns with dashboard — disabling autonomous heartbeat sets line `paused`; enabling sets `active`
- Update: Docker Compose PostgreSQL tuning — `max_connections` 200→400; `shared_buffers` 256MB→512MB (main and data-workspace Postgres services)
- Fix: Markdown PDF export — decorative Unicode separator lines (e.g. box-drawing `═`) render as rules instead of garbled text; removed redundant filename title from the first page; normalize line endings; ordered list markers no longer double the period; list text width uses full column after the number; page-break and wrap logic use bottom margin Y and avoid near-zero wrap width (fixes vertical one-letter lines and missing tail content)
- Fix: Agent task `update_task_status` — invalid transitions (e.g. from terminal `done`) return a structured tool error with a warning log instead of an exception stack trace; prompts and heartbeat briefings clarify valid transitions
- Feature: Agent Line timeline page — export full timeline as PDF or save as a Markdown document in the library (respects current filters)
- Feature: Line dashboard task preview uses compact one-line cards, scrollable columns after three tasks, and a footer link to the full task board
- Feature: Agent Line task cards show a hover tooltip with full title, description, assignee, creator, goal, status, priority, due date, and timestamps (tasks page and line dashboard)
- Feature: Agent line heartbeats only while an active/blocked goal has progress under 100%; reporting 100% completes the goal — reduces token use after all goals finish (use **Run Heartbeat** manually for a one-off wrap-up summary)
- Fix: Connections service — redact Telegram tokens in any log line that includes Bot API URLs; set `httpx`/`httpcore` to WARNING when `LOG_LEVEL` is above DEBUG so per-request lines stay quiet (set `LOG_LEVEL=DEBUG` to see them again, still redacted)
- Fix: Collapsed chat “Open Chat” control inset from the right edge so the main page scrollbar thumb stays draggable (Agent Lines and other full-width routes)
- Fix: gRPC `CreateAgentMessage` and `ReadAgentMessages` pass `line_id=` into `agent_message_service` (was invalid `team_id=` kwarg), so `send_to_agent` and inbox reads work for agent lines
- Fix: Web search default SearXNG engines use `google,bing` with `SEARXNG_ENGINES` override (avoid DuckDuckGo CAPTCHA empty results on self-hosted SearXNG)
- Fix: gRPC `SetWorkspaceEntry` / `ReadWorkspace` pass `line_id=` into `agent_workspace_service` (was invalid `team_id=` kwarg), so shared line workspace writes and reads persist correctly
- Fix: `ResolveAgentHandle` accepts `@prefix`, agent profile UUID, and unique case-insensitive display name (not only handle) so workers can message managers named in briefings
- Fix: Help docs vector sync manifest stored under `UPLOAD_DIR` so unchanged help files are not re-embedded on every container restart
- Update: Orchestrator and gRPC context use canonical metadata key `line_id` for agent lines (with `team_id` mirrored for legacy); `line_id_from_metadata` helper; heartbeat briefings and tool copy refer to line_id
- Fix: gRPC context gatherer maps `line_id` to `team_id` when only `line_id` is set so team heartbeat and line-scoped agent runs receive `team_tools` (create_task_for_agent, send_to_agent, etc.) in the orchestrator
- Fix: Team heartbeat CEO context calls `agent_task_service.list_line_tasks` (was `list_team_tasks`, missing on module) so task enumeration works for autonomous runs
- Fix: Agent line API and gRPC timeline helpers call `get_line_timeline` / `clear_line_timeline`, `delete_all_line_tasks`, `reset_line_goals_progress`, `clear_line_agent_memory` (stale `*_team_*` service names)
- Fix: Team heartbeat context SQL uses `agent_messages.line_id` (column was renamed from `team_id` in migration 101); pending-approval filter accepts `preview_data.line_id` or `preview_data.team_id`
- Fix: gRPC `CreateAgentTask` passes `line_id=` into `agent_task_service.create_task` (was `team_id=`, invalid keyword); orchestrator `create_task_for_agent` now works
- Update: Heartbeat `TEAM_TOOL_IDS_RULE` clarifies create_task_for_agent does not take task_id; pipeline executor logs truncated args for agent-line tools at INFO; tools-service logs CreateAgentTask outcomes
- Fix: Agent Line settings member add uses `lineId` in `TeamMembershipEditor` (parent passed `lineId` but editor read `teamId`, causing POST `/lines/undefined/members`)
- Fix: Agent Line goals `GoalEditor` accepts `lineId` (`lineId ?? teamId`) so create/update goal no longer POSTs to `/lines/undefined/goals`
- Fix: Line dashboard uses `updateLine` and `startLineDiscussion` (not `updateTeam` / `startTeamDiscussion`) for Activate Autonomous and moderated discussion
- Fix: Celery line heartbeat tasks call `agent_line_service.check_line_budget` (renamed from `check_team_budget`)
- Fix: Agent Line Quick team and related UI call `createLine` / `addLineMember` / `removeLineMember` / `getLineTimeline` / `resetLine` (renamed from Team APIs; fixes "createTeam is not a function")
- Update: Agent Factory sidebar create actions unified into dropdown menus (Agents, Playbooks, Data Connectors); connector export moved from sidebar to individual connector editor; playbook export added to playbook editor
- Update: Agent Factory sidebar — agents, playbooks, and data connectors listed by name (stable order); folder names sorted A–Z with Uncategorized last; items within each folder sorted by name
- Feature: Agent Factory sidebar categories — user-defined categories (folders) for Agents, Playbooks, Skills, and Data Connections; drag-and-drop items between categories; expand/collapse per section and per category persisted in localStorage
- Update: Universalize chat history for Custom Agents — metadata allowlist for persisted assistant messages (tool_call_summary, etc.); internal chat API now saves tool_call_summary; history injection only via {history} template variable; prompt_history_enabled rename (application-layer alias); enriched tool_call_summary for pending operations; proxy naming cleanup (is_custom_agent_request, skip_persistence)
- Update: Renamed Agent Teams to Agent Lines to align with Agent Factory paradigm
- Feature: Agent profiles gain a "Show in chat @ menu" toggle (chat_visible) — agents can have handles for inter-agent use without appearing in the user's chat @ dropdown
- Feature: Ctrl+Shift+J opens journal-for-the-day modal; Journal tab removed from Quick Capture (Ctrl+Shift+C)
- Feature: Agent Teams Phase 2 — multi-turn agent conversations with thread persistence and halt checks; team_tools pack auto-injected when team_id present; Team Settings page (heartbeat, governance); manual Run Heartbeat and Invoke Agent; governance approval loop (hire_agent/strategy_change); team approvals panel; timeline thread expansion and user message injection; richer heartbeat context (stalled tasks, unassigned goals, escalations, agent errors, pending approvals, previous summary)
- Fix: Agent Factory profiles with include_user_facts=False no longer get get_user_facts or save_user_fact tools (symmetric “vacuum” behavior: no read, no save)
- Feature: Agent Factory prompt variables expansion — new runtime variables (editor_filename, query_length, today_day_of_week, editor_is_first_section, editor_is_last_section, editor_ref_count); documented last_tool_results, document_context, pinned_document_id, editor_refs_*_previous/next, _iteration; Advanced conditionals (is defined, matches, AND/OR, step output expressions) in help and list_available_actions
- Feature: Agent Factory expression conditionals and editor_length — prompt conditionals support comparisons (e.g. {{#editor_length > 5000}}...{{/editor_length > 5000}}); editor_length variable added for open file character count so playbooks can choose full editor vs current section by size
- Fix: Playbook step merge matches by step identity (name/output_key) so inserting or reordering steps no longer misaligns prompts
- Update: Playbook tooling best-of-breed — get_playbook_detail includes full definition JSON in formatted output when under 45k chars; standardized tool descriptions and docstrings for list_playbooks, get_playbook_detail, create_playbook, update_playbook, delete_playbook, assign_playbook_to_agent, validate_playbook_wiring; list_available_actions adds Playbook definition shape and get_playbook_detail guidance
- Fix: Playbook get/update see and preserve all step fields — deep merge on update (merge_playbook_definition_steps) copies any old step field that is missing or empty in the incoming payload (prompts, params, condition, then_steps, else_steps, parallel_steps, phases, available_tools, max_iterations, skill_ids, etc.); get_playbook_detail formatted output now includes run_context, per-step hints (max_iterations, params, skill_ids, branch/parallel/phases), and an instruction to preserve all fields when editing
- Fix: Playbook updates no longer erase prompts in other steps — get_playbook_detail now includes prompt/prompt_template (and key step fields) in formatted output so the Engineer can preserve them; backend and gRPC update_playbook merge missing prompt/available_tools/phases/inputs from existing steps when the incoming step omits them
- Fix: Agent Factory custom agent now applies changes after approval — preview responses and list_available_actions instruct the agent to call the same tool again with confirmed=True when the user says yes; docstrings and register_action descriptions updated for confirm-flow tools
- Fix: GetPlaybook no longer crashes on non-UUID playbook_id; backend resolves by playbook name or slug (e.g. "Test Orch" or "test-orch") so get_playbook_detail works with id from list_playbooks or exact name
- Fix: Valhalla service moved to optional "routing" profile so default compose up does not fail when routing data is not set up; use `docker compose --profile routing up -d valhalla` after running scripts/setup_valhalla_data.sh
- Update: Agent Factory list tools (list_playbooks, list_agent_profiles, list_skills) now include both display name and UUID in formatted output so custom agents can use exact playbook/profile/skill IDs without parsing structured fields
- Fix: get_skill_detail / GetSkillsByIds no longer crash when given a non-UUID (e.g. "agent"); orchestrator validates skill_id and returns a clear message; backend filters invalid IDs before query
- Fix: Stale editor context — custom agent no longer loads active_editor from checkpoint when editor_preference=ignore; _clear_request_scoped_data called in format_response_node; pipeline node guards editor block by editor_preference to prevent 872K context bomb when using Agent Factory from non-Documents page
- Feature: Playbook prompt variable escape syntax — {literal:varname} emits literal {varname}; {raw_block}...{/raw_block} passes content through without resolution so engineer agent prompts can document variable names for created/edited playbooks
- Feature: Agent Factory full visibility tools — get_playbook_detail, get_agent_profile_detail, get_data_connector_detail, list_agent_schedules, list_agent_data_sources for engineer agent; list_playbooks and list_data_connectors responses enriched with category, tags, is_locked
- Feature: Data Workspace full SQL and write support — per-workspace PostgreSQL schemas with real tables for new tables (storage_type native); parameterized SQL ($1, $2) and INSERT/UPDATE/DELETE with write permission; rows_affected and RETURNING in API and query_data_workspace tool; row APIs (get/insert/update/delete) use native tables when storage_type is native; DDL (CREATE/ALTER/DROP) supported so users can run SQL scripts; workspace schema is source of truth — list_tables includes DDL-created tables, get_table/get_table_data accept optional database_id to resolve and load them
- Feature: Data Connector API key in URL — auth can send API key as query parameter (e.g. Alpha Vantage); connector definition supports `location` (header/query) and `param_name`; Connector Builder UI adds "Send key in" and "Query parameter name"; Alpha Vantage template added — edit proposals flow only via DB (patch_file → propose_document_edit → WebSocket → DocumentViewer); accept/reject call backend apply/reject endpoints; strict slice verification for replace/delete at apply time; SSE editor_operations path removed; EditorOpsPreviewModal and message-based re-push removed
- Feature: Near-gapless Subsonic playback — direct stream URL with token auth, next-track preload, and stream-proxy without preflight validation
- Feature: Add `matches` operator to playbook condition/branch evaluator for regex-based deterministic routing without LLM classify steps
- Update: Remove character caps from non-LLM paths — document cleaning, extraction, Celery result storage, RSS article content, connector probe response, crawl full_content, and MCP get_document now return full content; caps remain only where content is sent to an LLM (e.g. summarization prompts)
- Update: get_document_content_tool no longer truncates at 50K chars
- Feature: Reference scoping for playbook prompts — per-category variables `{editor_refs_CATEGORY_toc}`, `_current`, `_adjacent` follow manuscript heading; dynamic `{editor_refs_CATEGORY_section:Name}` extracts named sections from full reference content
- Update: LangGraph 1.x and langgraph-checkpoint-postgres 3.x — bump from langgraph 0.6.3 to >=1.0.0,<2.0.0 and checkpoint-postgres to >=3.0.0 for latest fixes and compatibility; v1 migration is backwards compatible (StateGraph, set_entry_point, AsyncPostgresSaver unchanged)
- Feature: Multi-provider key abstraction — admin providers (OpenAI, OpenRouter, Groq) from env; user providers in Settings with Groq support; ModelSourceResolver for available/enabled models and credential resolution; auto-switch to user models when first personal provider is added; model dropdowns show source/provider tags; Agent Factory stores model source metadata with soft-retarget and notices; provider capability guards (e.g. no reasoning extras for Groq)
- Feature: Comprehensive Agent Factory export/import — agent bundle includes watch_config, chat_history_*, persona (by name), and skills referenced by playbook steps; import restores profile fields, resolves persona by name, creates user skills from bundle and remaps step skill_ids; standalone YAML export/import for Skills and Data Connections with standard format (bastion_skills_bundle / bastion_connectors_bundle); Export/Import buttons in sidebar for Skills and Data Connections
- Feature: Expand in-app help documentation — 4 stubs expanded (Welcome, Next steps, Chat overview, Settings overview), 16 new topics (conversations, Org Quick Capture, editor features, models, external connections, teams and messaging, RSS and news, data workspace, knowledge graph, media, map and locations, control panes and status bar)
- Fix: Agent Factory persona wiring — custom agent profile saves now persist persona_mode/persona_id correctly, new agents default to account persona mode, and backend persona context handling no longer fails on users.id/user_id mismatch
- Feature: Chat UI updates — Send button now follows active accent palette and AI messages include a Read action for browser/system TTS playback
- Feature: Accent color themes and Appearance settings — ThemeContext stores and persists accentId (blue, teal, green, purple, orange); MUI theme and CodeMirror editors use accent for primary/links/selection; Settings → Appearance tab (mode Light/Dark, accent swatches, reset); Navigation menu "Appearance…" links to Settings Appearance
- Fix: Persistent event loop per Celery worker — single loop per process so async singletons (embedding service, gRPC clients, DB pool) no longer raise RuntimeError("Event loop is closed"); shared run_async() and worker_process_init/worker_shutdown hooks — retired built-in skill "help" (application assistance); add to retired_slugs, drop from orchestrator health/details and base_agent doc
- Feature: Dev note documenting future Data Connector Skills pattern — companion skills that encode domain expertise for using data connectors
- Fix: tools-service now receives SECRET_KEY so it can decrypt OAuth tokens stored by the backend (fixes "Failed to decrypt stored token" for calendar/email connections)
- Fix: Agent Factory agents with email connection tools (e.g. email:N:send_email) now automatically get list_emails, search_emails, list_email_folders, read_email, and search_contacts so those tools are available without adding them to each profile
- Fix: Local screenshot tool — image from local proxy is no longer truncated or dropped; pipeline executor intercepts image_data_uri, sends a short placeholder to the LLM, and attaches the screenshot to the agent response so it appears in chat
- Fix: Default playbook skill discovery — tools-service runs a delayed startup sync to populate the skills vector collection (so email and other built-in skills are found when auto_discover_skills runs); improved logging when lazy sync embeds 0 skills — tools-service runs a delayed startup sync to populate the skills vector collection (so email and other built-in skills are found when auto_discover_skills runs); improved logging when lazy sync embeds 0 skills
- Feature: Default AF ReAct agent — built-in Bastion Assistant profile and playbook (single llm_agent step with auto_discover_skills and research-oriented tools); visible in Agent Factory UI, permanently locked; routing uses default agent when no custom agent or editor pin; editor gating unchanged
- Feature: Seed all Python dict skills as built-in Agent Factory skills — 41 built-in skills (20 automation, 5 research, 13 editor, 3 procedural) seeded at startup; vectorized and searchable; retired slugs org-todo-management, email-composition, multi-source-research merged into task-management, email, research; tags and evidence_metadata stored for execution hints
- Update: Fact extraction tuned for universally applicable facts — prompt emphasizes cross-conversation usefulness; excludes topic-specific/situational context (e.g. home specs, one-off project details); max 2 facts per run, trigger every 20 messages
- Fix: Sticky custom agent chat history lookback — context_window_size now uses agent profile chat_history_lookback when sticky routing applies (was falling back to 6 after first @mention)
- Feature: Execute control action — agents can simulate control pane actions via execute_control_action_tool (gRPC); settings Add/Edit Control dialog has per-control Test button and response viewer with path picker for value_path
- Feature: Cohesive persona system — database-backed personas table (built-in + custom), default persona in user settings, free-form style instructions; Settings → Personas tab (default selector, create/edit/delete custom personas); Agent Factory profiles get three-mode persona (none / default / specific) with optional persona override — database-backed personas table (built-in + custom), default persona in user settings, free-form style instructions; Settings → Personas tab (default selector, create/edit/delete custom personas); Agent Factory profiles get three-mode persona (none / default / specific) with optional persona override
- Feature: Intelligent background fact extraction — durable user preferences extracted from conversation turns; throttled to every 6th user message plus last-chance extraction on conversation deletion
- Update: Episode (activity history) purge shortened from 90 days to 48 hours, runs every 6 hours instead of daily
- Feature: Browser scroll tool (browser_scroll) for infinite-scroll pages — direction (down/up), amount_pixels (default 800); added to browser tool pack and agent_factory_builder skill
- Feature: Browser automation as granular playbook tools — 11 session-scoped tools (browser_open_session, browser_navigate, browser_click, browser_fill, browser_wait, browser_extract, browser_inspect, browser_screenshot, browser_download_file, browser_close_session) with session persistence (encrypted browser_session_states), browser_inspect for selector discovery, browser tool pack and agent_factory_builder integration; Celery task check_browser_session_health every 30 minutes invalidates sessions that redirect to login
- Feature: Document version history (Phase 1) — full snapshots for editable documents (md, org, txt) on every save/agent edit/proposal apply; version list, view content, unified diff, and rollback in DocumentViewer; daily Celery task prunes old versions (90-day retention, keep every 10th, max 200 per doc)
- Refactor: Remove unused OSRM custom Dockerfile and entrypoint (compose uses upstream osrm-backend and map-tiles images)
- Feature: CLI worker Tier 1 tools — render_pdf_pages (pdftoppm), compress_pdf, convert_pdfa (Ghostscript), optimize_image (optipng/jpegoptim), generate_qr_code (qrencode), render_svg (librsvg); new cli_pdf_tools and cli_utility_tools; orchestrator tools registered with Action I/O Registry
- Feature: CLI worker service — dedicated cli-worker container for sandboxed CLI tool execution (FFmpeg, ImageMagick, Pandoc, Tesseract, Graphviz, ExifTool, Poppler, yt-dlp); gRPC API and orchestrator tools for transcode_media, extract_audio, get_media_info, download_media, read_media_metadata, convert_document, ocr_image, extract_pdf_text, split_pdf, merge_pdfs, convert_image, render_diagram; CreateUserFile supports optional binary_content for storing CLI output — LLM_Task and LLM_Agent steps can enable "Auto-discover skills"; at run time the step prompt is embedded and matched against a shared Qdrant skills collection (per-user payload filter); top-k skills are merged with manually assigned skills and injected as guidance + required tools. Single collection with user_id/__builtin__ filter; skill CRUD and seed_builtin_skills trigger embed/remove/sync.
- Feature: Agent Factory Skills system — procedural knowledge for LLM steps: built-in read-only skills (org-todo, document-search, research, email, knowledge-graph), user-authored skills with versioning, assignment at profile/playbook/step; skill procedures injected into system message and required_tools auto-bound; SkillsSection in Agent Editor, step-level skill picker in StepConfigDrawer, Skills list in sidebar; list_skills_tool, create_skill_tool, propose_skill_update_tool for agent self-improvement with HITL approval.
- Feature: Next-gen memory expansion — episodic memory (user_episodes) stores conversation-derived events via extract_episode_task after each turn; relevance-filtered injection as RECENT ACTIVITY in chat; Activity History in Settings with toggle and clear. Contradiction detection: agent overwrites of user-set facts queue as pending_review; user can accept/reject in Pending Fact Updates; full fact change history (user_fact_history) and REST endpoints for pending and history.
- Remove: Mumble voice server integration (mumble-server container, pymumble provider, and Settings UI)
- Update: Remove Deezer client from media sources; SubSonic and Audiobookshelf remain supported
- Feature: Universal browser tool (browser_run) — final action parameterized as download, click, extract, or screenshot; BrowserDownload RPC and browser_download_tool kept as aliases
- Feature: Games page with Solitaire (Klondike) and Lemonade Stand — /games hub with resume/new game; both games persist state to localStorage per user
- Docs: Add OPENFANG_COMPARISON.md — feature parity analysis vs OpenFang (channels, LLM providers, Hands, security, priorities)
- Fix: Sticky @handle routing for Telegram/bots — external chat saves agent metadata (primary_agent, agent_profile_id) after orchestrator response; orchestrator merges agent_profile_id from cache and falls back to conversation context so custom agent persists across turns
- Fix: Compound plan classification — anti-compound prompt rules and continuity bias reduce task+calendar and follow-up over-splits; post-validation collapses compound plans when the continuity skill appears in the plan (follow-up treated as single skill)
- Feature: Agent Factory execution debugger — step-level traces (inputs, outputs, status, duration) stored in agent_execution_steps; Execution History detail drawer shows step timeline with expandable inputs/outputs and Raw metadata accordion
- Feature: Playbook versioning and clone — auto-snapshot on playbook update to playbook_versions; Version History drawer in Playbook Editor with restore; Clone playbook action in sidebar
- Feature: Journal deletion — clear all runs or delete single execution from Execution History; DELETE /profiles/{id}/executions and DELETE /profiles/{id}/executions/{eid}
- Feature: Custom agent date/time context — custom agents receive current date/time in the user's timezone (same as built-in agents); Agent Profile toggle "Include date/time context" (default on) lets deterministic agents disable it; template vars {today}, {tomorrow}, {today_end} use user timezone when on, empty when off
- Update: Shorten Agent Factory tool list descriptions to one line; agent gets full schema when tools are bound
- Refactor: Stop registering internal document tools in Action I/O Registry — update_document_metadata, propose_document_edit, apply_operations_directly, apply_document_edit_proposal, load_file_by_path, load_referenced_files remain callable by code but no longer appear in Agent Factory tool list; document category shows only update_document_content, patch_file, append_to_file
- Refactor: Remove deprecated document_edit_proposal_tools.py (superseded by patch_file); overhaul AGENT_FACTORY_TOOLS.md to match actual tool registry
- Feature: Unified patch_file and append_to_file for Agent Factory — patch_file proposes batched edits (insert_after_heading, replace, delete, append) with resolution; append_to_file delegates to patch_file; both create DB proposals for user approval and are in the file_management tool pack
- Fix: Agent Factory llm_agent step tool selection retained after refresh — tools in available_tools that are not in the current actions list (e.g. after restart or different orchestrator response) are preserved when saving from the step config drawer; UI shows "Also selected (not in current list)" for retained tools
- Feature: Outline beats may include optional visual/atmosphere guidance for prose; fiction generation subgraphs treat beats as events plus prose-crafting guidance
- Feature: Custom agent editor operations — Agent Factory custom agents that return editor_operations (e.g. from a propose-updates tool or pipeline step) now emit editor_operations SSE chunks so proposed edits appear in the document diff UI; CustomAgentRunner collects editor_operations and manuscript_edit from pipeline step results and surfaces them in the response
- Feature: Editor toggle reflects pending edits for current file — message metadata now persists editor_document_id and editor_filename with editor_operations; chat sidebar "Prefer Editor" toggle shows primary (teal) when the conversation has pending edits in Postgres for the currently open document, with tooltip "Pending edits for this file (saved in conversation)"
- Docs: Add REMOTE_DATA_CATALOG_PLAN.md — architecture plan for catalog-service container, universal partitioned DB schema, and provider pattern for runtime NFS/SMB/S3 data source connections
- Feature: Refile todo agent tools — refile_todo and discover_refile_targets exposed via gRPC and task_management tool pack for Agent Factory and pre-built agents
- Feature: Agent Factory handle optional — agents without a handle are schedule/Run-only (not @mentionable in chat); create/update and Identity allow blank handle; list_handles and resolve exclude them; UI shows "Schedule only" where @handle would appear
- Fix: O365 connection reliability — ConnectionsServiceClient reconnects on gRPC failure (stale channel after connections-service restart); connection_status set to token_error on refresh failure and active on success; refresh_all_expired_oauth_tokens covers all OAuth connection types (e.g. microsoft email/calendar)
- Feature: Neo4j entity cross-reference tools and Entity Graph visualization — Agent Factory tools find_documents_by_entities, find_related_documents_by_entities, find_co_occurring_entities (knowledge_graph pack); GET /api/graph/entity-graph and EntityRelationGraph component with entity/document nodes and co-occurrence edges; Documents menu entry "Entity Graph (knowledge map)"
- Feature: ReAct and automation engine tool wrapping use Action I/O Registry — descriptions, per-parameter descriptions from inputs_model, optional params_model merge, and Returns hint from outputs; pipeline executor and automation engine both use shared rich metadata for bind_tools
- Feature: Unified contacts in Agent Factory — get_contacts tool gathers from both O365 and org-mode (include_org=true by default); backend get_contacts_unified merges sources with source badge; same pattern as calendar unification
- Feature: Playbook wiring fixes — pipeline injects {today}, {today_end}, {tomorrow} into inputs; list_available_actions_tool appends wiring rules (single-brace syntax, output_key.field, runtime vars); GetCalendarEventsInputs defaults for start/end_datetime to avoid false required-field warnings
- Feature: get_my_profile tool — agents can look up the current user's email, display name, preferred name, timezone, ZIP, and AI context; GetMyProfile gRPC and backend handler; agent_factory_builder skill uses it when building agents that need user contact info
- Fix: Todo toggle supports org headings at any level (***, ****, etc.) and surfaces clear error when line is not a task
- Feature: O365 Contacts support — Microsoft Graph Contacts API in connections-service; backend contacts API and LangGraph/orchestrator contact tools; Contacts view merges O365 and org-mode contacts with source badge; Contacts.ReadWrite scope
- Feature: Available AI models list is user-scoped when using own API keys — non-admins who toggle "Use my own API keys" now see models from their connected providers in Settings and in the chat model dropdown instead of the admin OpenRouter list
- Feature: IMAP/SMTP and CalDAV connections in Settings — user can add IMAP/SMTP email (Gmail, Fastmail, etc.) and CalDAV calendars (Google, Nextcloud, iCloud) in the Connections pane; Email Connections section includes Office 365 and IMAP/SMTP; Calendar Connections section for CalDAV; Agenda view supports CalDAV sources
- Feature: Multi-provider embedding support in Vector Service — OpenAI, OpenRouter, Ollama, and VLLM selectable via EMBEDDING_PROVIDER; EMBEDDING_DIMENSIONS env var (default 3072); provider-aware validation and cache key by model
- Feature: Twilio SMS outbound messaging — connect Twilio (Account SID, Auth Token, From Number) in External Connections; agents can send SMS via SendOutboundMessage (provider=sms, recipient_chat_id=phone); connections-service SMSProvider, backend /api/connections/sms, frontend SMS dialog
- Feature: Agent Factory meta-tools — LLM-callable tools to create agents, playbooks, schedules, and data source bindings from natural language; list_available_actions, create_agent_profile, create_playbook, assign_playbook_to_agent, create_agent_schedule, bind_data_source_to_agent; HITL via confirmed=False preview then confirmed=True; agent_factory_builder skill; backend agent_factory_service layer and gRPC handlers
- Feature: Tool category chips in chat message footer — shows which tool categories (Search, Email, Calendar, etc.) were used during a response, using existing Action I/O Registry categories
- Fix: Remove deprecated org/todo tools (update_todo_state_tool, list_todos_by_tag_tool, list_org_todos_tool, org_inbox_* CRUD) that competed with working universal todo API; LLM agents now only see todo_tools.py (list, create, update, toggle, delete, archive)
- Feature: O365 calendar support alongside email — list calendars, get events (date range), get event by ID, create/update/delete events via Microsoft Graph; connections-service provider, backend client and tool service handlers, orchestrator calendar tools and calendar skill; Calendars.ReadWrite scope; confirmation flow for create/update/delete
- Update: Expanded FUTURE_TOOLING.md with document format production tools (DOCX, XLSX, PPTX creation), chart generation, Mermaid diagrams, Markdown→Office conversion, and composed workflow patterns sourced from Anthropic Skills and OpenClaw ecosystems
- Feature: External connections auto-restore and health check — connections-service self-restores chat bots on startup via GET /api/internal/active-chat-bots; Celery Beat sync_chat_bot_connections every 2 minutes re-registers stopped bots
- Feature: Custom agent auto-routing — agent_profiles.auto_routable toggle (default off); when enabled, the agent is registered as a skill and can be selected by the LLM skill selector for matching queries without @mention; ListAutoRoutableProfiles gRPC and load_auto_routable_agents with 60s TTL; CUSTOM_AGENT handler in unified_dispatch; Identity section toggle in Agent Editor
- Feature: Document search excludes image sidecars — image .metadata.json chunks are tagged with is_image_sidecar in the vector payload; document search applies a not_equals filter so only text documents are returned; image search unchanged; run scripts/backfill_image_sidecar_flags.py and restart file watcher to backfill existing sidecars
- Feature: Paginated web crawling — crawl_web_content tool supports paginate, max_pages, pagination_param, start_page, next_page_css_selector, and css_selector; backend wrapper loops over pages (param-based or selector-based); WebCrawlResult now includes images and links
- Feature: Per-profile persona toggle for Agent Factory custom agents — persona_enabled on agent_profiles; when enabled, LLM task and LLM agent playbook steps receive a system message with the user's persona (name, style, preferences); Identity section toggle in Agent Editor
- Feature: Per-profile chat history for Agent Factory custom agents — chat_history_enabled and chat_history_lookback on agent_profiles; LLM task and LLM agent playbook steps receive conversation history when enabled; Identity section toggle and lookback field; pipeline executor prepends history to LLM calls
- Feature: Sticky @handle routing — agent_profile_id saved to conversation metadata after custom agent runs; follow-up messages without @mention auto-route to the same custom agent; context gatherer loads saved profile_id when no @handle in message
- Fix: Bump pydantic to 2.12.5 across all containers for ocrmypdf 17.2.0 compatibility (backend, celery-beat/flower/worker, llm-orchestrator, vector-service, crawl4ai-service)
- Fix: OCR PDF processing — upgrade ocrmypdf to 17.2.0 (pikepdf 10 compatible); register path during OCR so file watcher skips re-processing; file watcher uses full reprocess pipeline for binary files (PDF, DOCX, etc.) instead of reading as UTF-8; add deskew/clean/optimize to ocrmypdf for better scan quality
- Feature: Connection-scoped email tools for multi-account support — bind email accounts to agent profiles; playbook gets scoped actions (email:<connection_id>:send_email, etc.) so Tool and LLM Agent steps can target a specific account; agent_service_bindings table, CRUD and available-email-connections API, Service Bindings section in Agent Editor, pipeline executor email: prefix handling
- Fix: LLM Agent step tool list no longer wraps into hidden columns (flexWrap: nowrap on FormGroup) so all tools are visible and scrollable
- Fix: Workflow Composer prompt placeholder wiring no longer mutates the template — stores reference in step inputs instead, preventing phantom input fields that cannot be deleted
- Refactor: Remove chat agent's internal research handoff — chat now returns task_status=rejected; orchestrator retry loop runs LLM skill re-selection (excluding rejected skills) instead of blind fallback to research
- Fix: Automation engine injects active_editor content into LLM messages for requires_editor skills (org_content, reference) — previously only injected into tool params, causing LLM to hallucinate without seeing the file
- Fix: Playbook editor shows data connector actions when opened from sidebar by using first agent that uses the playbook; hint shows which agent's connectors are shown
- Fix: Tool action dropdown is searchable (Autocomplete) in Add Step panel and Step config so many connector actions do not require long scrolling
- Feature: Self-managed planning tools (create_plan, get_plan, update_plan_step, add_plan_step) for LLM multi-step workflows; Zone 1 in-memory store, Action I/O Registry, planning tool pack for Agent Factory and automation engine
- Feature: Research agent quality evaluation — post-synthesis evaluation node (fast model) rates response sufficient/insufficient/off_topic; one re-synthesis with feedback when quality is poor
- Feature: Plan Engine quality gate and re-plan — rule-based check after each level for failed/empty step results; when a dependency is unusable and remaining steps depend on it, re-plan remaining work via fast model (at most once per run)
- Feature: Automation Engine multi-iteration tools — up to 3 rounds of tool calls per skill invocation; _execute_tool_calls_round helper; skills can chain tools (e.g. search then refine)
- Feature: Dynamic LangGraph playbooks — Custom Agent playbooks run as real LangGraph workflows; each step is a node; approval and loop steps use interrupt_after; playbook_graph_builder builds graph from steps; CustomAgentRunner invokes dynamic graph and resumes via checkpointer on approval
- Feature: LLM Agent step type — playbook step that lets the LLM select from multiple tools at runtime; ReAct loop with bind_tools; available_tools, max_iterations, prompt_template, model_override, output_schema; AddStepPanel LLM Agent tab with multi-select tool picker; StepConfigDrawer config for tools, prompt, max iterations, model override, output schema
- Feature: Bot chat switching — /chats lists recent conversations with numbers; /loadchat N switches to that conversation (Telegram/Discord/Slack); /chat shows current conversation; selection persisted in provider_metadata so it survives restarts
- Feature: pick_random_file tool — Pick a random document from a folder (Zone 2: Tools Service); optional file_extension filter (e.g. png, jpg) for comics/images; proto PickRandomDocumentFromFolder, backend gRPC handler, orchestrator tool and Action I/O Registry
- Refactor: Data source connector execution moved to connections-service — connector HTTP execution (connector_executor) runs in connections-service; backend resolves credentials and delegates via ExecuteConnectorEndpoint gRPC; Zone 3 rule updated to cover all third-party connections
- Feature: Agent YAML bundle export/import — export an agent with its default playbook and data source connectors as a single YAML file; import creates connectors, playbook, profile, and bindings; Export YAML in editor and on agent cards; Import button in Agents sidebar
- Feature: Agent Factory sidebar three-section resource management — Agents, Playbooks, and Data Sources as collapsible sections; standalone Playbook and Data Source editors with usage warning banners; edit playbook/connector from agent or from sidebar
- Docs: Added Future Tooling dev notes — 18 assessed skill integrations (Slack, Google Sheets, Linear, Google Calendar, PDF enhancement, audio transcription, bookmarks, Home Assistant, Todoist, IMAP/SMTP, YouTube transcripts, Readwise, Spotify, Reddit, Plex, Prometheus, meal planning, Bitwarden) with zone placement, dependency impact, and implementation steps
- Update: Notification lifecycle — read/unread styling, mark-as-read on click, per-item dismiss (X), "Clear all" link; clearing conversations already clears notifications
- Feature: In-app notifications show agent profile name — send_channel_message accepts agent_name param; pipeline executor auto-injects agent_profile_name from custom agent profile; notification bell displays profile name instead of generic "Agent"
- Update: Clicking an in-app notification opens the chat sidebar and selects that conversation in-place (no navigate to /chat); sidebar expands if collapsed
- Fix: Clearing all conversations also clears the notification bell list so no orphaned notifications remain
- Fix: Agent Factory test runs no longer create a sidebar "log" conversation — backend skips persisting user/assistant messages when agent_profile_id is set; frontend uses a transient conversation ID and no longer creates a conversation for test runs; output is only via explicit send_channel_message/notify_user and Recent Runs
- Feature: send_channel_message supports in_app channel — creates in-app conversation message for the current user (chat sidebar + notification bell); StepConfigDrawer adds In-app conversation option and hides bot/recipient params when in_app is selected
- Feature: Victoria plugin (VictoriaMetrics + VictoriaLogs) with 10 typed tools for metrics queries, log search, facets, and observability
- Feature: send_channel_message adds email channel with To/From (system SMTP or user account), channel dropdown (Telegram/Discord/Email); Output Destinations pane removed from Agent Factory (use workflow steps instead)
- Feature: Agent Factory pipeline loop and inline output steps — loop step runs child steps up to max_iterations; output step fires to notification (or other) destination mid-pipeline; StepConfigDrawer and WorkflowSection support loop/output with nested child editing
- Feature: @mention routing for Agent Factory — type @handle in chat (web, Telegram, Discord) to route to a custom agent; context gatherer resolves handle to agent_profile_id and strips prefix; GET /api/agent-factory/handles for autocomplete; chat input shows @mention dropdown with arrow-key and click selection
- Update: Agent Factory plugins — remove standalone Plugins section; configure plugin credentials inline in step drawer when a plugin action is added to a workflow (Connect/Reconfigure per step)
- Feature: Telegram /chatid and known-chats for send_channel_message — bot replies to /chatid or /chat_id with current chat ID, type, and title; every message records the chat in provider_metadata.known_chats; GET /api/connections/:id/known-chats and recipient dropdown in StepConfigDrawer so users can pick a chat that has messaged the bot
- Feature: send_channel_message recipient — optional recipient_chat_id param (Telegram chat_id or Discord channel id) for spontaneous messages; when set, message is sent to that chat; when empty, uses stored outbound_chat_id (last chat that messaged the bot). Proto and clients updated across connections-service, backend, orchestrator; StepConfigDrawer hint explains usage.
- Feature: Agent Factory step params and send_channel_message UX — persist step.params in playbook definition; StepConfigDrawer shows Params section for tool steps (params_schema); send_channel_message gets channel, connection_id (dropdown of user chat_bot connections), format; notification output destination hint and routing log for diagnostics
- Fix: provider_metadata JSONB returned as string by asyncpg — parse defensively in get_outbound_chat_id, update_outbound_chat_id, and refresh_access_token so spontaneous agent messaging via Telegram/Discord works
- Fix: Custom agent profile output_config may be string (from API) — parse before .get() in custom_agent_runner; LogAgentExecution timestamps passed as datetime to asyncpg
- Fix: Agent Factory JSONB double-encoding — asyncpg re-encoded json.dumps strings for JSONB columns, causing definition/triggers to be stored as JSON strings instead of objects; added explicit ::jsonb casts on all INSERT/UPDATE parameters and _ensure_json_obj safety parsing on all read paths
- Fix: Agent Factory workflow steps disappearing after adding an action — sanitize definition to prevent data ballooning; guard WorkflowSection render with playbookId fallback; reset local state on profile switch; use RETURNING in playbook creation to prevent name-collision reads; add backend definition-size logging
- Feature: Agent Factory tool I/O enrichment and new tools — Search & Discovery (expand_query, search_conversation_cache entries_count); Enhancement (entries_count, explicit output fields); file_analysis optional avg_words_per_sentence/paragraph; org_content get_org_statistics total_headings/todo_count/completion_rate; schedule_reminder and create_todo/list_todos_by_tag/update_todo_state (task management) with full I/O models and registry; notification_tools schedule_reminder_tool (stub until backend support)
- Feature: Agent Factory enable/disable — pause/resume endpoints for agent profiles; status indicators and quick toggle in sidebar; Active/Paused switch in editor; scheduled execution respects profile is_active
- Update: Agent Factory tool I/O enrichment — list_available_formulas output model and registration with formatted; data_workspace list/schema/query outputs add count, rows, columns, workspace_id/workspace_name; rss refresh adds new_items/count; duplicate StartTask/GetTaskStatus/ApprovePermission/GetPendingPermissions/HealthCheck removed from grpc_service.py
- Feature: Agent-initiated multi-channel notifications — notify_user tool creates in-app conversation + delivers to Telegram/Discord/email; send_channel_message for single-channel delivery; SendOutboundMessage gRPC on connections-service; outbound chat_id persistence; output_router multi-channel support; agent-initiated conversation badge in sidebar
- Feature: Email-from selection for agent outputs — notification destination and send_email support from_source: system (Bastion SMTP, default) or user's email connection; Agent Factory UI adds Notification destination type with channels and "Email from: System / My account"

## [0.50.0] - 2026-03-24
- Update: Bump version to 0.50.0 across application and Docker

## [0.20.0] - 2025-02-15
- Update: Bump version to 0.20.0 across application and Docker

## [0.16.0] - 2025-01-XX
- Feature: Import generated images into the document library
- Refactor: Simplify fiction agent
- Fix: Prompt multiplication bloat in Fiction agent
- Feature: ePub export (untested)
- Feature: 'Cover' frontmatter type support in Fiction documents

## [Unreleased]
- Feature: CustomAgentRunner and Output Router — LangGraph workflow (load_profile, prepare_context, execute_pipeline, route_outputs, format_response); interrupt_before approval_gate; output_router document and data_workspace destinations; unified_dispatch uses CustomAgentRunner.process(); GetActionsResponse includes typed ActionContract/ActionField; analyze_image_query, detect_faces_in_image, identify_faces_in_image migrated to unified I/O; TodoItem, EmailRef, NotificationResult, ImageRef in tool_type_models
- Feature: Agent Factory custom agent execution — dispatch_custom_agent loads profile/playbook via GetAgentProfile/GetPlaybook gRPC, runs pipeline (tool + llm_task steps), streams results; LogAgentExecution gRPC writes agent_execution_log; tool_io_map backfill on automation skills; search_within_document/find_documents_by_tags registered; math_tools/navigation_tools fixes
- Feature: Agent Factory Phase 0 — database tables (agent_profiles, agent_data_sources, agent_skills, custom_playbooks, agent_execution_log, agent_discoveries, data_source_connectors), CRUD API for profiles/data-sources/playbooks, CUSTOM_AGENT engine type and routing when agent_profile_id in metadata
- Feature: Tool I/O migration tracker reconciled — status [r] (registered but shallow) and [x] (complete) applied; Tier 1/2/3 classification documented
- Feature: P5 PlanEngine typed_outputs — automation engine collects tool result dicts, dispatcher passes typed_outputs in complete chunk metadata, plan engine extracts and stores in ContextBridge for compound-plan step wiring
- Feature: Tool Registry Migration — P5–P8 and Phase 2A/2B tool migrations; ContextBridge typed_outputs; Skill tool_io_map; ToolPack get_aggregate_outputs; pipeline_executor; GET /api/agent-factory/actions; org, email, navigation, RSS, text_transform, data_workspace, image_generation, weather, session_memory tools return unified dict with formatted
- Feature: Agent Factory journal-as-debugger — journal entries capture full diagnostic context (execution trace, failure details, connector diagnostics, LLM step diagnostics) so users can ask agents about failures in natural language
- Feature: Agent Factory schema versioning — immutable output_schema versions for connectors; old versions retained while referenced by playbooks; breaking change detection with Assembly Agent auto-migration assistance
- Feature: Agent Factory per-step model selection — each LLM task step can specify which model to use with fallback chain (step → profile → user default → system default); cost estimates shown in Workflow Composer UI
- Feature: Agent Factory connector sharing & cross-agent rate limiting — rate limits enforced at connector instance level (shared across all agents using same connector); connector library for reuse across agents
- Feature: Agent Factory agent-to-agent communication — handoff, request-response, and conversational loop patterns via agent message bus; real-time observation in chat sidebar and external messaging; team-aware agents that monitor and comment on team posts; safety guardrails (max turns, daily limits, same-owner requirement)
- Feature: Agent Factory UX design document — module-card + configuration-drawer pattern (no React Flow); vertical step cards with type-aware input wiring dropdowns; Assembly Agent chat co-pilot integration; component hierarchy mapping to existing MUI/React architecture
- Feature: Tool I/O type system with automatic coercion — 8 core types (text, number, boolean, date, record, list, file_ref, any); text as universal connector; coercion rules prevent type mismatches from blocking valid tool chains; unified dict return with mandatory formatted field eliminates dual _tool/_structured variants; Action I/O Registry with type compatibility checking; migration tracker for 81 existing tools across 18 categories
- Feature: File/Folder Service separation analysis — architecture assessment for dedicated file-service container with phased plan, security/reliability/performance trade-offs, and decision matrix in dev-notes
- Fix: Skill loading idempotency — load_all_skills() guarded so safe to call multiple times; grpc_service uses _ensure_skills_loaded() to avoid re-registering 33 skills on every request
- Feature: Ranked routing with fallback stack — skill selector returns top-3 ranked skills; on task_status=rejected orchestration retries with next fallback (no extra LLM call); help/define short-circuits get default fallbacks
- Feature: Ranked routing with fallback stack — skill selector returns top-3 ranked skills; on rejected, orchestration retries with next fallback (no extra LLM call); help/define short-circuits get default fallbacks
- Feature: Help-to-Research handoff when help skill detects out-of-scope question (domain knowledge); routing scopes help to application-only
- Feature: Agent Factory design document and technical implementation guide — comprehensive plan for user-defined research agents with declarative data source connectors, composable skills/playbooks, configurable output destinations (documents, folders, Data Workspace tables, knowledge graph), entity resolution pipeline, knowledge accumulation loop, and AI-powered Assembly Agent; supersedes Data Workspace Analytics Enhancements
- Feature: Agent Factory step I/O contracts — typed input/output schemas for all actions and connector endpoints; Workflow Composer uses contracts for visual step wiring with type checking; built-in action I/O registry; step linking validation at save time
- Feature: Agent Factory @mention invocation — custom agents have unique @handles; users type @handle in chat to invoke; sidebar shows My Agents panel with quick-action buttons; no auto-routing for custom agents
- Feature: Agent Factory work journal — agents maintain a persistent human-readable activity log; users can ask agents about past work; write_journal_entry and query_journal tools; journal entries searchable by date, entity, and status
- Feature: Agent Factory team integration — agents can be shared with teams; configurable team file and post access; team tools (search_team_files, write_team_post, summarize_team_thread); team journal visibility with scoped access; credential isolation preserved
- Feature: Agent Factory monitor mode — periodic change-aware polling (heartbeat pattern); detection tools (detect_new_files, detect_folder_changes, detect_new_data, detect_new_team_posts, detect_new_entities) with persistent watermarks; smart suppression when nothing changed; Celery Beat integration
- Feature: Agent Factory examples document — 16 end-to-end use cases covering all patterns (deterministic pipelines, LLM-augmented research, hybrid investigations, scheduled reports, folder monitors, API monitors, team discussion summarizers, image classifiers, meeting minutes task extractor); decision guide for choosing patterns
- Feature: Agent Factory tool catalog document — comprehensive inventory of 81 tools across 12 categories (file operations, search, task management, notifications, knowledge graph, data workspace, text processing, web, email, monitor, agent internal, external integrations); typed I/O contracts for all tools; scope-aware access control for file operations
- Feature: Agent Factory modular plugin registry — BaseToolPlugin interface for external integrations; PluginLoader with auto-discovery; plugin I/O contracts registered in action registry; database tables for plugin config; API endpoints for plugin management
- Feature: Agent Factory external integrations — Trello (6 tools), Notion (6 tools), Slack (3 tools), CalDAV (4 tools) plugin definitions with full I/O contracts; extensible to GitHub, Jira, Google Sheets, Airtable
- Feature: Agent Factory notification and messaging tools — send_notification (in-app), send_channel_message (Telegram/Discord/Slack via connections-service), broadcast_to_team; granular task management tools (update_todo, complete_todo, search_todos); granular file operations (delete_file, move_file, copy_file)
- Feature: Agent Factory workflow composition model — agents compose from three step types (deterministic tools, LLM tasks, approval gates) in any mix; three execution modes (deterministic/LLM-augmented/hybrid); three run contexts (interactive/background/scheduled); approval gates with LangGraph interrupt_before for interactive and PostgreSQL notification queue for background
- Fix: Gate electronics skill to active editor (requires_editor=True) so technical questions without an electronics doc open route to research
- Fix: Frontmatter stripping only removes leading YAML block (use \\A anchor) so manuscript scene breaks (---) are never stripped; fixes whole-manuscript analysis losing middle chapters
- Feature: Paragraph-numbered two-phase fiction editing — editing existing chapters uses Phase 1 (identify paragraphs to change) and Phase 2 (batched rewrites); system-extracted original_text eliminates anchor hallucination; new chapter generation unchanged
- Fix: Fiction request-type detection — queries with action words (revise, edit, fix, improve, etc.) route to edit path and produce editor operations even when phrased as questions; pure informational questions stay question-only
- Fix: Fiction editor granularity — align 10-20 word guidance in generation subgraph with system prompt; clarify replacement-text completeness; add cross-chapter edit guard (original_text/anchor_text must not span chapter boundaries)
- Feature: Two-pass article (non-fiction) generation — keyword "two-pass" in article/substack/blog requests runs draft pass (raw article text) then refinement pass with style guide and outline for compliance
- Feature: Two-pass chapter generation for fiction — keyword "two-pass" when creating a new chapter runs draft pass (creative prose only) then refinement pass (tighten, style compliance, ManuscriptEdit JSON)
- Fix: Fiction generation subgraph preserves invalid_anchor_operations in shared_memory so self-heal and merge nodes receive them when top-level state key is dropped
- Fix: Fiction whole-story intent uses negative guards so edit/revise/rewrite and "this chapter" keep chapter scope; remove generic whole-story phrases (discrepancies, cohesive, overall)
- Fix: Strip ManuscriptEdit JSON from conversation history in fiction editing subgraphs so LLM does not recycle stale original_text anchors
- Update: Electronics agent suggests new reference files (suggested_files in response) instead of auto-creating them; content routing and save nodes no longer create files
- Fix: Capture-to-inbox uses org_capture only (no research) unless user explicitly asks for research first; inbox is for short idea capture
- Fix: org_capture skill uses prior_step_*_response from shared_memory when run as step 2 in a compound plan so the researched content is captured, not the user's short phrase
- Feature: org_capture converts research and prior-step content to org-mode syntax (headings, bold/italic, tables, lists) for neat inbox entries
- Fix: File relation graph restricted to current user's My Documents only; RLS role forced to user and all queries filter by user_id so nodes/links never leak across users
- Feature: "Craft the next chapter" / "Write next chapter" — resolves target from cursor position (e.g. cursor in Ch1 → generate Ch2)
- Fix: Honor explicit "craft/generate Chapter N" over cursor position when that chapter does not exist yet (e.g. "Craft Chapter 2" with only Chapter 1 in manuscript)
- Feature: Obsidian-style file relation cloud (link graph) with RLS-secured document_links, tab and fullscreen UI, backfill endpoint
- Feature: Download my Library button zips and downloads My Documents folder/file structure
- Update: Overhauled fiction generation prompting — Style Guide elevated as primary creative authority, outline reframed as story ingredients not a recipe, added scene-thinking instructions to prevent beat-by-beat expansion
- Fix: Improved skill routing for editor-context queries to prevent compound plan false positives
- Fix: Enhanced fiction_editing skill description to include analyze/review capabilities
- Fix: Narrowed story_analysis skill to writing theory/concepts (not manuscript review)
- Fix: Added editor-context-takes-precedence-over-continuity guidance to skill selection
- Update: Skills architecture design — add Phase 7 (Compound Query Planner), Phase 8 (Tool Packs), Phase 9 (Hierarchical Selection), new tools/skills roadmap, OpenClaw comparison
- Fix: Skill dispatch (unified_dispatch) routes electronics, general_project, podcast_script to dedicated agents instead of Writing Assistant; Writing Assistant only when active_editor present
- Fix: Skill selection prefers research for how-to questions when no editor; research skill description/keywords include how do I, how to, how can I
- Refactor: Fiction only via Writing Assistant; remove standalone fiction_editing_agent workflow and dispatch; remove build_context_preparation_subgraph and build_resolution_subgraph; fiction_editing_subgraph unchanged
- Refactor: Remove standalone agents (outline, character, rules, style, article); routing and labels use Writing Assistant only; subgraphs unchanged
- Refactor: Disable technical hyperspace agent; /hyperspace and cached technical_hyperspace route to chat (implementation plan kept in docs/dev-notes)
- Fix: Include structured images in research complete chunk when using skill dispatch (unified_dispatch)
- Fix: Route local collection queries (e.g. Dilbert comics, photos of X) to research skill instead of entertainment
- Fix: Org content agent now correctly routes via AUTOMATION skill instead of CONVERSATIONAL (prefer editor with org file open now works)
- Fix: Fiction chapter generation state flow — return validated_operations from generation subgraph merge node for resolution subgraph, fix duplicate system_prompt key overwriting in build_generation_prompt_node
- Fix: Fiction chapter generation – normalize single-operation LLM response in validate_generated_output_node, preserve manuscript/context in _detect_request_type_node early return, always return editor_operations/manuscript_edit from fiction format_response for Writing Assistant
- Refactor: LLM-primary skill routing — filter_eligible (hard gates) + llm_select_skill (descriptions); remove keyword scoring and domain detection
- Refactor: Remove standalone proofreading_agent; proofreading remains as subgraph in fiction_editing, article_writing, podcast_script
- Refactor: Remove standalone rules_editing_agent; rules editing handled by Writing Assistant → rules_editing_subgraph only
- Fix: Faster new AI chat creation (prime cache, skip checkpoint for new conversations, skip redundant refetch)
- Fix: Clarified structured image pattern - frontend uses EITHER structured images OR markdown, not both (prevents duplicate display)
- Feature: Migration 040 adds external_connections, connection_sync_state, connection_data_cache, system_settings for existing DBs (OAuth / email / chat bots)
- Feature: Passive security analysis agent (exposed files, headers, info disclosure) via Tools Service and LLM Orchestrator
- Feature: URL in AI chat sidebar extracts single linked page by default; full-site crawl only when user says crawl/capture/ingest/scrape
- Feature: Route fiction documents through Writing Assistant Agent via unified fiction_editing_subgraph; intent classifier routes type: fiction to writing_assistant_agent
- Refactor: Remove standalone article_writing_agent and character_development_agent; article/character flows use Writing Assistant subgraphs only
- Documentation: Added Technical Hyperspace Implementation Plan for multi-domain simulation system
- Documentation: Added comprehensive Data Workspace Analytics Enhancements planning document covering 4 phases of advanced pattern detection, correlation discovery, and real-time analytics
- Feature: Image search runs hybrid (user + global) when user_id is set; regular users see their images and Global
- Feature: Collection search results show Why it matches and full description in a modal (Read more)
- Fix: Duplicate date removed from collection result captions (title already contains date)
- Fix: Folder delete from disk (file watcher) now uses explicit admin RLS context for global folders
- Fix: Images now display from local document searches via structured metadata transmission
- Fix: PostgreSQL full-text search fallback with automatic stemming (handles brain/brains, dog/dogs plurals)
- Fix: Text search fallback now allows documents with empty type field (database sync issue workaround)
- Fix: Document repository no longer overwrites metadata_json with quality_metrics during INSERT
- Fix: Image metadata now stores both 'type' and 'image_type' keys for reliable search filtering
- Fix: ImageSidecarService now uses correct fetch_one() import, preventing duplicate document creation
- Fix: ImageSidecarService now uses correct column name 'processing_status' instead of 'status'
- Debug: Enhanced vector search logging to diagnose embedding/retrieval issues
- Fix: Vector deletion timeouts when deleting multiple files - added semaphore to limit concurrent deletions to 5, retry logic with exponential backoff for CANCELLED errors, and increased timeout to 120s
- Fix: gRPC keepalive configuration prevents connection drops during bulk vector operations
- Feature: LLM-powered query classification for research agent (collection_search, factual_query, exploratory_search) with fast paths
- Feature: Collection search subgraph with rich metadata descriptions (title, date, series, author, content, tags)
- Feature: Improved image search with canonical content_type handling (comics->comic, photos->photo)
- **BREAKING**: Image search now uses `/api/documents/{id}/file` endpoint instead of hardcoded paths (images served from actual document location)
- Fix: Image search exact match fallback now only triggers with explicit date filter (prevents random results)
- Fix: Image search returns "no results" when concept doesn't match, instead of random comics from series
- Fix: Removed hardcoded directory structure assumptions - images use document folder structure
- Fix: Changed image-vision-service host port from 50056 to 50057 to avoid port conflict
- Feature: Research agent fast path now returns rich item details (title/filename, content preview) and standardized image emission
- Update: Fast path builds structured_images from image documents when image search returns none; response includes markdown images for Chat Sidebar
- Update: Chat Sidebar image URL handling supports /api/documents/{id}/file for research result images
- Feature: Research agent three-tier routing (fast/standard/web) for simple existence queries
- Update: Fast path skips query expansion, sufficiency check, visualization, and full doc analysis
- Update: Query expansion skipped for simple queries (<=5 words)
- Update: Image query analyzer skipped for simple existence queries and cached for parallel searches
- Update: Visualization skipped for simple queries (short query and short response)
- Fix: Fast path returns structured_images and image_search_results for display
- Feature: Benchmark tests for research tier routing (test_research_tier_routing.py)
- Feature: Extended image metadata schema with type-specific fields (location, event, medium, body_part, modality, map_type, coordinates, application, platform)
- Update: Enhanced Edit Image Metadata UI to show type-specific fields based on image type selection
- Update: Image metadata sidecars now support richer searchability for photos, artwork, medical images, maps, and screenshots
- Fix: CRITICAL - Character development subgraph conversation history fallback was too aggressive (triggered at <200 chars), causing recreation of entire profiles instead of adding to existing content
- Fix: Changed character subgraph conversation history threshold from <200 chars to ==0 (completely empty files only)
- Fix: Added comprehensive debug logging to character development subgraph for content tracking
- Fix: CRITICAL - Changed guidance from "replace ENTIRE bullet list" to "replace ONLY THE LAST BULLET" for surgical precision
- Fix: CRITICAL - Updated all subgraphs (rules, style, character, outline) with surgical bullet addition guidance (1-2 bullets max in original_text)
- Fix: CRITICAL - Added validation warnings for overly broad replace_range operations (5+ bullets, 500+ chars) to prevent deleting headings/unrelated content
- Fix: CRITICAL - Enhanced _validate_operation_for_bullet_lists() to detect both insert_after violations AND overly broad replacements
- Fix: CRITICAL - Updated decision trees, examples, and guidance to emphasize surgical approach: "Only include what you need to modify"
- Fix: CRITICAL - Added programmatic enforcement in rules_editing_subgraph to REJECT insert_after operations with bullet content
- Fix: CRITICAL - Created _validate_operation_for_bullet_lists() function that detects and blocks forbidden insert_after+bullet combinations before resolution
- Fix: CRITICAL - Prohibited insert_after for bullet list additions across all writing subgraphs (rules, style, outline, character)
- Fix: Enhanced editor operation prompts with decision tree showing correct approach: replace entire bullet list with expanded version using replace_range
- Fix: Updated 4 subgraphs (rules, style, outline, character) to enforce replace_range for bullet additions - eliminates middle-insertion issue at source
- Fix: Added comprehensive visual examples showing WHY insert_after fails for bullets (picks wrong anchor → splits list) vs WHY replace_range succeeds (clean expansion)
- Fix: CRITICAL - Implemented programmatic section boundary detection in rules_editing_subgraph to prevent insertions in the middle of sections
- Feature: Rules subgraph now automatically corrects anchor_text to use the actual last bullet in the target section before resolution
- Fix: Added _find_section_bounds, _find_last_bullet_in_section, and _correct_anchor_for_section functions for robust section detection
- Refactor: Added domain-specific editor guidance for Rules, Style, and Outline documents with semantic section mapping and automatic placement logic
- Refactor: Migrated style_editing_subgraph and outline_editing_subgraph to use centralized editor operation instructions from shared utilities
- Feature: Rules documents now have semantic section mapping (bride hierarchy → Institutions & Power Dynamics, magic → Magic Systems, etc.)
- Feature: Style documents now have section relationship awareness (POV guidelines affect both Point of View and Narrative Voice sections)
- Feature: Outline documents now have chapter structure templates, 70-beat limit enforcement, and beat placement strategy guidance
- Fix: CRITICAL - Enhanced MISTAKE #7 with multi-line bullet example showing how using "last line of middle bullet" still splits lists
- Fix: Added 4 verification checks for bullet lists including "scan ENTIRE section" and "verify next content is header, not another bullet"
- Fix: Strengthened operation type guidance for insert_after to require "LAST LINE of LAST BULLET" with explicit scan requirement
- Fix: Added explicit warning "Using last line of middle bullet splits the list" to prevent using end of any bullet as anchor
- Fix: CRITICAL - Added MISTAKE #7 "Using middle bullet as anchor" with visual example showing how insert_after splits lists when not using LAST bullet
- Fix: Added explicit guidance "FOR BULLET LISTS" in 3 locations: safety rules, operation types, and verification checklist
- Fix: Enhanced insert_after operation type to warn "Using middle bullet splits the list - new bullets inserted between existing ones!"
- Fix: Added 2 verification checks specifically for bullet list additions (anchor must be LAST bullet, verify no bullets after anchor)
- Fix: CRITICAL - Added explicit START/END markers around file content to prevent LLM from using anchor text from wrong sections (fixes operations defaulting to beginning of document)
- Fix: Added MISTAKE #6 for "anchor text from wrong section" with visual example (prevents using text from style guides/outlines instead of actual file)
- Fix: Enhanced verification checklist to check anchor text EXISTS "between START/END markers" (not from reference docs)
- Fix: Rules subgraph now wraps file content with "=== START OF RULES CONTENT ===" markers for clear text source boundaries
- Fix: CRITICAL - Incorporated fiction agent's battle-tested editor operation guidance into standardized utilities for rock-solid in-editor diffs
- Fix: Added "USE MINIMAL MATCHES" guidance with concrete word-level examples (prevent over-broad original_text that loses adjacent content)
- Fix: Added "WHAT IS VALID ANCHOR TEXT" section with explicit ✅ CORRECT / ❌ WRONG patterns
- Fix: Enhanced verification checklist with "mental Ctrl+F" check and 15 specific validation points
- Fix: Expanded common mistakes to 5 concrete failure scenarios with visual BAD vs GOOD examples
- Fix: Strengthened language from "unreliable" to "will FAIL" to emphasize criticality of text anchors
- Fix: Added "REPLACEMENT TEXT IS COMPLETE" warning with concrete example showing how partial text gets lost
- Fix: CRITICAL - Added explicit "TEXT ANCHORS MANDATORY" guidance to prevent LLMs from generating index-only operations that stomp wrong content
- Refactor: Created centralized editor operation instruction utilities in writing_subgraph_utilities.py with reusable prompt components
- Refactor: Migrated rules_editing_subgraph to use standardized editor operation instructions from shared utilities
- Fix: Enhanced rules agent prompts with explicit "PREFER GRANULAR EDITS" guidance to prevent over-broad replace_range operations that delete unrelated content
- Fix: Added operation validation logging in rules agent to warn when original_text is too large (>300 chars), contains multiple bullet points, or includes section headers
- Fix: Added comprehensive verification checklist and common mistake examples to rules agent prompts
- Feature: Standardized agent response contract with AgentResponse model for consistent response structure across all agents
- Feature: Unified response extraction helpers in grpc_service supporting both legacy and standard formats during migration
- Feature: Rule documenting agent response contract with examples and migration guidelines
- Feature: Added `images` field to AgentResponse contract for structured image data (supports text + image responses)
- Refactor: Migrated reference_agent, fiction_editing_agent, outline_editing_agent, and image_generation_agent to use standard AgentResponse contract
- Fix: Reference agent now formats pattern analysis and insights data into readable lists in chat responses instead of only showing summaries
- Fix: Increased outline agent max_tokens to 250K to allow unlimited operations without truncation
- Fix: Added truncation detection with helpful error messages and detailed logging for incomplete LLM responses
- Fix: Removed artificial operation limits to allow large multi-chapter edits in single request
- Fix: Critical bug in editor operation resolver where undefined variable caused operations to be placed at beginning of file
- Fix: Editor operation resolver now handles None values in start/end positions to prevent type comparison errors
- Fix: Outline agent now correctly inserts subsections (### Status, ### Pacing) immediately after chapter headings instead of at end of chapter
- Fix: Added detailed debug logging to editor operation resolver to diagnose chapter boundary detection issues
- Fix: Improved outline agent error handling for empty LLM responses with better diagnostics
- Fix: Removed spurious frontmatter parsing warnings for simple file path fields
- Documentation: Added comprehensive future email integration guide covering Gmail and Office 365 OAuth implementation
- Fix: Resolved OrgMode editor decoration and folding issues by implementing proper CodeMirror 6 fold/unfold effects with Ctrl+Shift+H keybinding
- Fix: Intent classifier now respects action intent changes (analysis → modification) to prevent routing revisions back to story_analysis_agent
- Fix: Updated btu_hvac formula to handle rooms list and calculate total BTU for multi-room reference documents
- Fix: Updated llm-orchestrator Dockerfile to generate all required gRPC protos including vector_service
- Fix: Installed Chromium and dependencies in tools-service Dockerfile to enable static chart generation via Kaleido
- Update: Increased research agent quick vector search timeout to 5 seconds to accommodate network latency
- Fix: Resolved fiction generation subgraph state preservation bug where generation_context_parts was dropped across nodes
- Fix: Added failed_operations conversion in fiction generation to display unplaceable content in chat sidebar
- Feature: Added context-aware fuzzy matching with auto-correction for fiction agent anchor texts to prevent LLM hallucination errors
- Fix: Resolved message normalization bug where editor_operations buried in metadata were not extracted to top level for display
- Feature: Implemented automatic text-splitting for large editor operations into 8K char chunks at paragraph boundaries to prevent nginx proxy truncation and maintain clean word boundaries
- Fix: Added chunk_index sorting to frontend operation application to ensure text chunks are applied in correct sequence
- Fix: Removed obsolete chunk reversal logic in backend that was causing text chunks to be sent in reverse order
- Fix: Added explicit instructions for fiction agent to use insert_after (not insert_after_heading) when continuing existing chapters to prevent duplication
- Fix: Modified editor operation resolver to prefer matches near cursor position (±10K chars) to ensure edits target the correct chapter when cursor is positioned
- Fix: Resolved DOCX file reading errors in gRPC GetDocumentContent by using DocumentProcessor for binary formats
- Feature: Added automatic location fallback to user ZIP code for weather tools
- Feature: Added date range support for historical weather queries (e.g., "2022-10 to 2024-02")
- Fix: Resolved visualization tool TypeError in orchestrator gRPC client
- Fix: Enhanced chart generation to support static SVG versions via gRPC
- Fix: Fixed gRPC double-abort error in weather service error handling
- Feature: Added "Technical Hyperspace" Deterministic System Modeling Engine with gRPC topology service and NetworkX graph traversal
- Feature: Added Systems Engineering Agent for technical design and failure simulation analysis
- Refactor: Modularized monolithic backend/main.py into domain-specific API routers
- Refactor: Simplified PDF processing to focus on automated text extraction and vectorization
- Cleanup: Decommissioned obsolete manual OCR, coordinate-based PDF editing, and layout segmentation units
- Feature: Moved ePub cover page to the end of the spine to prevent "double cover" in readers
- Fix: Enhanced ePub cover resolution to support relative paths in frontmatter and database lookups
- Fix: Resolved XML syntax error in ePub cover page generation
- Update: Created Org-Mode enhancement roadmap in docs/dev-notes
- Fix: Resolved issue where 'Filter by Tag' in All TODOs view was empty by correcting data property access
- Update: Centralized all weather intelligence logic into dedicated gRPC Tools Service
- Refactor: Migrated Weather Agent and Status Bar API to use gRPC weather service, eliminating redundant logic
- Feature: Added real-time moon phase and meteorological metadata to central weather tool
- Feature: Extracted dedicated gRPC Tools Service container from backend monolith
- Update: Reconfigured LLM Orchestrator to communicate directly with Tools Service via gRPC
- Refactor: Trimmed backend container size by moving tool logic to microservice
- Feature: Added image import functionality for generated images - users can now import images directly into document library folders
- Feature: Automatic cleanup of generated images when conversations are deleted - imported images are preserved, non-imported images are removed
- Fix: Implemented "Smart Validation" for editor diffs to prevent invalidation during full-document syncs
- Fix: Resolved race condition and network-sync related diff invalidation in the live editor
- Fix: Resolved race condition where rapid acceptance of multiple live diffs caused remaining diffs to disappear
- Fix: Prevented live edit diffs from disappearing when accepting multiple changes in rapid succession
- Fix: Resolved frontend build error due to missing variable definition in MessagingContext
- Fix: Resolved message duplication and unread count persistence issues in real-time chat
- Fix: Resolved issue where new rooms and initial messages failed to appear for recipients in real-time
- Fix: Resolved issue where user "offline" status failed to broadcast in real-time
- Fix: Resolved issue where users remained "online" after logging out or closing their browser
- Fix: Improved WebSocket headcount logic to correctly track multiple simultaneous connections per user
- Fix: Resolved issue where unread message counts would reappear after a page refresh
- Fix: Resolved issue with messaging presence indicators getting stuck or failing on refresh
- Fix: Resolved issue where messaging notifications failed to update until the drawer was opened
- Fix: Resolved circular RLS dependency in messaging system causing rooms to vanish on refresh
- Fix: Corrected room display name calculation to correctly identify other participants
- Feature: Added detailed logging for outline chapter extraction in fiction agent
- Fix: Improved forward-looking outline context for new manuscripts in fiction generator
- Refactor: Established standard agent handoff pattern using shared_memory for clean inter-agent data passing
- Update: Simplified intent classification to trust LLM semantic understanding over pattern matching
- Fix: Research agent now receives reference document context when delegated by reference agent
- Fix: Added 'reference' to allowed editor types in frontend for proper reference agent routing
- Fix: Backend now accepts reference documents even if not editable for reference_agent analysis
- Refactor: Removed unnecessary short-circuit routing hack, using normal intent classification
- Feature: Added in-editor suggestion capabilities for editor-interactive agents (fiction, outline, electronics)
- Feature: Media service integrations in progress (SubSonic, Audiobookshelf, Deezer)
- Fix: Fiction agent continuity file creation now uses same folder resolution as electronics agent (folder_id or folder_path from canonical_path)
- Fix: Enhanced cursor detection with complete gRPC proto support for fiction editing agent
- Fix: Chat message copy now preserves markdown formatting as rich text (HTML) while keeping raw markdown syntax for plain text editors
- Feature: Added Dictionary Agent with short-circuit routing for "/define" queries (instant lexicographic lookups)
- Update: Changed dictionary agent trigger from "define:" to "/define" command format
- Fix: Resolved conversation ID mismatch causing 403 errors when accessing conversations from checkpoint list
- Feature: Added universal DocumentEditBatch system for atomic multi-edit operations
- Feature: Added intelligent reference file creation to general and electronics agents (>1500 char threshold)
- Refactor: Migrated electronics_agent to use batch editing for atomic, efficient document updates
- Update: Enhanced both project agents with automatic file creation and frontmatter management
- Fix: Resolved FictionEditingState NameError by using forward references for type-safe state access helpers
- Fix: Improved type safety in fiction editing agent with Pydantic model conversion for structured_edit and continuity_state
- Fix: Enhanced operation resolution in fiction editing agent with better anchor text matching and error handling
- Fix: Active editor frontmatter now persists correctly during typing - removed re-parsing that was overwriting cached frontmatter with empty data

### Bug Fixes
- Fix: Resolved KeyError in electronics agent when user approves pending save operations
  - Root cause: Conditional edges from analyze_intent node missing 'save' and 'maintenance' destinations
  - Added missing routing paths for approval flow resumption

### Conversation History Persistence Fix
- **Fix**: Resolved critical conversation history issue where LLM orchestrator was treating every request as a new conversation
  - Root cause: Backend was not loading conversation state from database before sending to LLM orchestrator
  - Added `_load_conversation_state()` function to retrieve `primary_agent_selected` and conversation context
  - Intent classifier now properly maintains agent continuity between requests
  - Conversation history now persists correctly across LLM interactions
- **Fix**: LLM orchestrator agents now properly save conversations to LangGraph checkpoints matching backend behavior
  - Root cause: Agents were not adding current user query to messages before workflow invocation
  - Root cause: Agents were not adding assistant responses to messages after generation
  - Added `_prepare_messages_with_query()` helper to BaseAgent for consistent message preparation
  - Added `_add_assistant_response_to_messages()` helper to BaseAgent for checkpoint persistence
  - Updated all critical agents (chat, electronics, data_formatting, help, content_analysis) to use helpers
  - User queries and assistant responses now properly saved to PostgreSQL checkpoints
  - Created test script to verify checkpoint persistence (`test_checkpoint_persistence.py`)

### Architectural Refactoring - Vector Service Only (No Fallback)
- **Breaking**: Committed fully to Vector Service architecture (no fallback to legacy EmbeddingManager)
  - All embedding generation routes through Vector Service microservice (gRPC)
  - EmbeddingManager replaced with deprecation stub (raises NotImplementedError)
  - Removed 300+ lines of duplicate embedding generation logic
  - Single source of truth: Vector Service for all embeddings
- **Refactor**: Simplified EmbeddingServiceWrapper to Vector Service only
  - Removed fallback logic and USE_VECTOR_SERVICE flag checks
  - Always uses Vector Service for embedding generation
  - Always uses VectorStoreService for storage/search
  - Reduced from 330 lines to 350 lines (cleaner, simpler)
- **Refactor**: Updated ResilientEmbeddingManager to use EmbeddingServiceWrapper
  - No longer directly depends on EmbeddingManager
  - Uses embedding_service for generation, vector_store for storage

### Architectural Refactoring - Vector Storage Separation
- **Feature**: Created VectorStoreService for clean separation of concerns
  - New service handles all vector database operations (Qdrant)
  - Collection management, point insertion, vector search, deletion
  - Foundation for multi-backend support (Milvus, Elasticsearch)
- **Refactor**: Updated search tools to use new architecture
  - Tools generate embeddings then search via VectorStoreService
- **Breaking**: Removed unused ParallelEmbeddingManager dead code (710 lines)
- **Breaking**: Removed FreeFormNotesService and all related code including database table (1001 lines total)
- Refactor: Query expansion extracted to standalone QueryExpansionService with dedicated LangGraph tool
- Refactor: Removed automatic query expansion from EmbeddingManager (agents now explicitly control expansion)
- Refactor: Removed unused ParallelEmbeddingManager dead code
- Refactor: Removed FreeFormNotesService and all related code including database table (unused feature with no UI)

## [0.1.0] - 2025-11-13

### Initial Release
A comprehensive AI-powered personal workspace platform featuring 25+ specialized agents for knowledge management, creative writing, data workspaces, collaboration, and productivity - all controlled through natural language.

#### Core Features
- **LangGraph Multi-Agent System**: 25+ specialized agents with PostgreSQL state persistence
- **Document Management**: RAG with vector search (Qdrant) and knowledge graphs (Neo4j)
- **Data Workspaces**: Dedicated microservice with PostgreSQL for custom databases, data import (CSV/JSON/Excel), and table management
- **Real-time Collaboration**: Messaging system with presence indicators and optional encryption
- **Org-mode Integration**: WebDAV sync with dedicated inbox and project agents
- **RSS Feed Management**: Background polling and article import with dedicated agent
- **Creative Writing Suite**: Fiction editing, proofreading, character development, and outline editing agents
- **Knowledge Integration**: Entertainment knowledge base
- **Specialized Agents**: Weather, image generation, audio transcription, coding assistance, research, fact-checking, and more
- **Microservices Architecture**: Docker Compose deployment with vector-service, data-service, and llm-orchestrator

#### Technical Stack
- Backend: Python, FastAPI, LangGraph, Celery, gRPC
- Frontend: React, Material-UI, WebSockets
- Databases: PostgreSQL (main + data workspaces), Qdrant (vectors), Neo4j (knowledge graph)
- Infrastructure: Docker Compose, microservices architecture
- AI: OpenRouter/OpenAI integration with structured Pydantic outputs



