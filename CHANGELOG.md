# Changelog

All notable changes to Bastion AI Workspace will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

- Fix: **Bastion Mobile** — RSS and EPUB readers: semibold body text, no font smoothing, wider line height and tracking for e-ink; RSS defaults (serif, 20px) and max font size 32px; EPUB defaults 20px.

## [0.70.14] - 2026-05-02

- Feature: **eBooks (web, mobile, BBS)** — Directional KoSync: per-device local reading position; when cloud is meaningfully behind local, web and mobile open at local with a dismissible banner to jump to sync; BBS uses one prompt only when session and sync chapters differ by 2+.
- Fix: **Bastion Mobile** — Full player: album art uses a square frame with `contain` (no top crop); Stop closes the now playing modal.
- Update: **Bastion Mobile** — Documents: recently opened (local), hybrid search modal, single-tap folder browse; document list uses correct `skip` query; RSS article reader uses a fixed header and bottom settings sheet (no WebView gesture overlay), removes FAB and pageSheet dismiss conflict.
- Fix: **Bastion Mobile** — appearance preference: sync read from SecureStore and `Appearance.setColorScheme` before first paint; valid storage key (`app.appearancePreference`); write failures propagate; `NavigationThemeBridge` gates on `hydrated`.

## [0.70.13] - 2026-05-02

- Update: **Bastion Mobile** — chat shows a working indicator, server status lines while streaming (including after tokens start), and a wider pending assistant bubble; RSS article chrome uses a back chevron and swipe-down on the controls sheet to dismiss.

## [0.70.12] - 2026-05-01

- Feature: **Agent Factory playbooks** — optional per-step `auto_apply` (boolean): on unattended runs (`scheduled`, `folder_watch`, team dispatch, etc.), `patch_file` applies edits directly via document-service instead of creating a proposal; `ApplyOperationsDirectlyRequest` adds `playbook_auto_apply`; folder-watch Celery dispatch sets `trigger_type: folder_watch`.
- Feature: **Microsoft Teams bot** — connections-service exposes `POST /teams/webhook/{connection_id}` (aiohttp) alongside gRPC; `POST /connections/teams` registers Azure Bot App ID + secret and returns `webhook_url`; notification cascade supports `teams`.
- Fix: **Bastion Mobile** — Appearance `resolvedScheme` follows saved light/dark/system choice for navigation and status bar; RSS prefs (feed source, mark-read-on-scroll, article reader font size/family/theme) use a single `RssPrefsProvider` with SecureStore hydration so settings stay in sync across launches and screens.
- Fix: **Bastion Mobile** — Documents list and detail reader use theme colors in dark mode; RSS article modal truncates the title to one line and adds font size (A−/A+), Sans/Serif/Mono, and Auto/Light/Sepia/Dark controls for the article WebView.
- Feature: **Emby playback** — `PlaybackInfo` accepts optional `device_profile` (Bastion forwards to Emby); web sends a browser device profile with HEVC-aware HLS fallback; mobile sends a mobile profile, picks HLS vs progressive from `TranscodingUrl`, reports correct `PlayMethod`, uses `expo-video` with `expo-av` retained for voice recording only; `Video` / `MusicVideo` items open on mobile.
- Fix: **Chat sidebar** — Stop removes the in-flight user + assistant rows, restores the composer draft, and deletes the persisted user message on cancel; stream emits `user_message_persisted` for id sync; partial assistant is not saved when the run was user-cancelled.
- Fix: **Chat sidebar** — composer clears immediately on send (including during streams); message list scrolls to bottom when a new user message is appended so the sent message stays in view.
- Update: **eBook reader (web)** — header shows the full title; KoSync digest and book icon removed; Theme, Font, and Size (X-Small through X-Large) plus fullscreen and Hide align on one row; prior font sizes snap to the nearest preset when loading saved prefs.
- Fix: OPDS search proxy URL allowlist now permits any path on the catalog origin (same scheme and host); directory-only restriction caused HTTP 400 for OpenSearch result URLs outside the root feed folder.

- Feature: **Chat stop and follow-up** — Stop clears LangGraph checkpoints for the conversation (and playbook) thread so the next send starts clean; Models settings add queue vs stop-and-send while streaming; sidebar composer stays enabled during streams with optional queued follow-up.
- Fix: **Bastion Mobile auth branding** — `ios.splash` / `android.splash` dark variants so prebuild emits night splash resources (dark mode was still using light art); auth header shows `bastion.png` / `bastion-dark.png` via Metro `watchFolders` on the shared frontend images.
- Feature: **OPDS eBooks** — PDF acquisition links in catalogs (parser + web/mobile OPDS pickers); web reader uses `react-pdf` for PDF bytes; mobile reader uses pdf.js from CDN in WebView; `acquisition_format` on recently-opened entries; KoSync remains EPUB-only.
- Feature: **Chat export** — markdown-aware DOCX, export options (font, size, paper, orientation), Markdown download; sidebar PDF/DOCX/Markdown uses live messages.
- Feature: **Markdown editor** — Download menu adds "Export as DOCX…" (same options dialog as chat); body exports without YAML fence, title from frontmatter or filename.

## [0.70.11] - 2026-04-30

- Fix: **Music / Audiobookshelf** — `normalize_track` duration no longer drops to zero when `runtimeTicks` is absent; audiobook chapters and podcast episodes expose correct lengths; mobile track rows hide unknown duration instead of showing `0:00`.
- Fix: **Bastion Mobile eBooks** — EPUB reader loads from disk in the WebView (`file://` + relative `fetch`) instead of embedding base64 in HTML, avoiding Android OOM on large books when reopening from cache.
- Feature: **Bastion Mobile / Emby** — HLS video with resume and playback reporting; home library chips, library grid, series season accordion, Emby-wide search; dock/mini player hidden during fullscreen video; music tabs hidden on Emby source; carousel layout fixes.
- Feature: **Bastion Mobile Media** — Audiobookshelf-aware labels (books/authors/podcasts), chapter and episode metadata, sorted podcast episodes; Emby home (continue / recently added), list cover thumbnails, and Emby playback reporting during audio playback.
- Fix: **Bastion Mobile eBooks** — reader font size, theme, and font family persist to server `reader_prefs` (debounced) so they survive restarts and apply across books; in-reader font picker (Georgia, Literata, Iowan, system UI, UI serif/sans) with WebView typography wired through epubjs themes.
- Fix: **eBook reader (web)** — replaced margin chevron buttons with transparent left/right tap strips (subtle edge gradient on hover), matching common reader UX; arrow keys unchanged.
- Fix: **OPDS (web)** — OpenSearch `rel="search"` links that point at descriptor XML are resolved to real `{searchTerms}` templates on the server; the web hub keeps the catalog search URL while browsing subfeeds; Browse adds scrolling, breadcrumbs, a two-column grid, and clearer subtitles for navigation entries.
- Fix: **Audiobookshelf** — cover art proxy uses `/api/items/{libraryItemId}/cover` with API token; sync stores library item IDs in `cover_art_id` (not `coverPath`) for albums, chapters, and podcast episodes.
- Fix: **Bastion Mobile** — top safe area (`ScreenShell` + EPUB WebView inset) on headerless screens and the reader so content clears the status bar, notch, Dynamic Island, and camera cutouts.
- Fix: **Bastion Mobile** — boot `index` route uses `ScreenShell`; slide-up modals (chat, todos, RSS source picker) use `useModalSheetBottomPadding` for the home indicator; voice capture modal respects top/bottom insets.
- Update: **Bastion Mobile ToDos** — single scope control (tap/long-press) opens a sheet with Inbox, all files, or one file; last choice persisted (SecureStore); new installs default to Inbox.
- Fix: **Music (web + API)** — audio stream proxy forwards `Content-Length` only with `Content-Range` (partial); full responses stream chunked so upstream disconnects no longer trigger Uvicorn `Response content shorter than Content-Length`. Track lists inherit album `cover_art_id` when the server omits per-track art; Media page header and status bar resolve art via normalized IDs, loaded tracks, and queue enrichment.
- Fix: **Music cover proxy HTTP 500** — cover IDs stored as TEXT (`169_music_cover_art_id_text.sql` for existing DBs; greenfield `01`/`168` updated); on cache read/write/DB errors the handler falls back to upstream getCoverArt; ENOENT on blob files clears stale index rows; clearer error logging.
- Feature: **BBS** — Telnet/SSH main menu adds E-Books (OPDS): browse catalogs from web-configured OPDS, search when supported, read EPUB chapters as plain text with the existing pager, optional KoSync progress (KOReader-compatible partial MD5 digest).
- Fix: **Music cover proxy** — upstream getCoverArt uses bounded concurrency, longer read/connect timeouts, and retries on 502/503/504 to avoid Navidrome stampede timeouts when the Media UI loads many thumbnails at once.
- Fix: **Music (web)** — status bar cover art uses absolute cover-proxy URLs, 64px size, and an error fallback icon instead of a broken image; shuffle tooltips describe next-track / queue behavior.
- Update: **Bastion Mobile ToDos** — org cards show created date only (no duplicate log preview), compact first note + “+N more”, status chip tap/long-press for toggle vs state sheet, redundant path line removed.
- Fix: **Bastion Mobile** — document FAB chat passes parsed YAML `frontmatter` into `active_editor` (via `parseFrontmatter` + route param) so orchestrator editor-type / playbook routing matches the web editor.
- Fix: **eBooks / KoSync** — web and mobile EPUB readers detect `epubcfi(` progress vs other clients (e.g. KOReader), fall back to `percentage` + `cfiFromPercentage` / `SEEK_PERCENT`, and use `locations.generate(1024)` on both for aligned percentage indexing.
- Fix: **Bastion Mobile** — app launcher hides the Media tile when there are no configured active music sources; refetches each time the launcher opens; shared `filterActiveConfiguredSources` with the Media library screen.
- Feature: **Music** — cover-art proxy uses long-lived immutable cache headers, deterministic `ETag` / `304` on `If-None-Match`, per-user on-disk LRU cache with Postgres index, bounded Celery warming after library refresh, and purge on source removal (shared Docker volume).
- Fix: **Music cover proxy + Bastion Mobile** — `304` only when a cache index row exists (avoids blank art from native revalidation); mobile player art drops `expo-image` `cacheKey` for JWT URLs; offline UI uses status copy, long-press to remove, track rows show “On device”, downloads list shows “Saved on device” and long-press to delete.
- Fix: **Bastion Mobile** — document chat `active_editor` uses a filename with an allowed extension, top-level `document_id`, and cursor fields so the backend forwards editor context; streaming `status` chunks show as placeholder text in the assistant bubble until content arrives.
- Feature: **Bastion Mobile Media** — disk-backed list cache (stale-while-revalidate) for library and album/playlist/artist views; manual refresh clears cache; `expo-image` disk cache for now-playing art from the cover proxy URL.
- Feature: **Media (web)** — draggable right edge on the library sidebar (220–600px) with `SplitResizeHandle`, persisted as `mediaPageSidebarWidth`.
- Fix: **Media (web)** — opaque `solidSurfaceBg` on the source tab strip, library sidebar, and album/playlist header bar so UI wallpaper does not bleed through those chrome areas.
- Fix: **Navigation** — pinned routes under `/media` are no longer dropped on refresh while music sources are still loading (prune only after `mediaSources` has finished fetching).
- Fix: **Music (web)** — album sidebar secondary line shows a compact artist credit (first names + “+N” for long VA lists) with single-line ellipsis instead of every contributing artist.
- Fix: **Music (web)** — sync `selectedView` with the active media tab when tabs or `activeTab` change so the sidebar does not stay on Playlists under Albums after source/tab strip updates; lazy-load cover images; stable list keys + `ListItemText` flex in album list.
- Feature: **Music (web)** — album art via `MediaService.getCoverArtUrl`: status bar thumbnail, album sidebar thumbnails, album detail header art, and Media Session artwork from `cover_art_id`.
- Fix: **Bastion Mobile** — document FAB chat: lazy conversation creation, no duplicate user message vs stream persistence, guarded reload after stream, and route conversationId loads messages explicitly.
- Feature: **Music / Subsonic** — `/api/music/cover-art/{cover_art_id}` proxy (JWT like stream-proxy); Bastion Mobile passes `cover_art_id` into Track Player `artwork` for mini and full player art.
- Update: **Bastion Mobile EAS** — iOS build profiles use EAS image `macos-sequoia-15.6-xcode-26.2` (Xcode 26) for App Store submission compliance.
- Feature: **Bastion Mobile Media** — `/(app)/media` library (artists/albums/playlists, search, multi-source), offline downloads, mini player + full-screen player via `react-native-track-player` (background playback and remote controls), and download manager; iOS `UIBackgroundModes` audio and Android foreground service permissions added in `app.config.ts`.
- Fix: **Bastion Mobile** — ToDos `results` memoized to stop an empty-array re-render loop; app launcher sheet `FlatList` scrolls; media library uses virtualized `FlatList` instead of rendering entire catalogs in a `ScrollView`.
- Feature: **Bastion Mobile eBooks** — OPDS browse/search, EPUB reader (WebView + epubjs), local file cache, KoSync progress, and settings for catalogs/KoSync at `/(app)/ebooks` (launcher + Settings link).
- Fix: **Bastion Mobile eBooks** — EPUB reader embeds book as base64 in WebView HTML (`source={{ html }}`) instead of `file://` + `current.epub` fetch so content renders on Android.
- Fix: **Bastion Mobile eBooks** — reader HTML loads JSZip and epubjs via synchronous `<script src>` in `<head>` so the epubjs UMD build always sees global `JSZip`.
- Fix: **Bastion Mobile eBooks** — EPUB reader uses `spread: 'none'` for single-column pages on phones; OPDS catalog chips use horizontal `FlatList` with bounded height and browse `FlatList` uses `flex: 1` for reliable scrolling.
- Fix: **Bastion Mobile eBooks** — EPUB reader attaches swipe listeners via epubjs `rendition.hooks.content` (iframe document) so horizontal page turns work; bundled JSZip/epubjs in WebView HTML removes CDN latency on each open.
- Feature: **Bastion Mobile eBooks** — reader uses left/right third PanResponder zones for tap/swipe page turns and swipe-down for chrome; middle third passes touches to the WebView for in-book links; center swipe-down opens chrome via `OPEN_CHROME` postMessage; chrome dismisses on tap/swipe-up on the dim backdrop.
- Update: **Bastion Mobile splash** — `expo-splash-screen` config plugin uses `frontend/public/images/bastion.png` vs `bastion-dark.png` with light/dark backgrounds (`#f5f5f5` / `#121212`) matching web `theme-color`; legacy single `splash` block removed.
- Fix: **Bastion Mobile RSS reader** — WebView article HTML uses `getColors` palette (body/background, links, blockquote/code/pre) so article text stays readable in dark mode (injected CSS previously fixed body text to `#111` while the WebView used a dark canvas, so only links stood out).
- Update: **Frontend theming** — aligned chat markdown/code and graph/map fallback text colors to theme-aware palette tokens; added light/dark `theme-color` + Apple touch/startup image metadata in `frontend/index.html` and runtime sync in `ThemeContext` for consistent splash/chrome branding.
- Fix: **RSS article viewer (web)** — article body and previews use MUI `palette.text` so content stays readable in dark mode; global `.rss-article-content` rules set explicit `color` on common tags plus dark `pre`/`code` surfaces.
- Update: **Bastion Mobile** — voice capture from a dock mic button; current section name is a pill that opens the app launcher (removed floating voice FAB and separate Open pill).
- Feature: **Bastion Mobile Settings** — `/(app)/home` as **Settings** (dock + launcher); sections **Account**, **Appearance** (system/light/dark, SecureStore + `Appearance.setColorScheme` + Navigation `ThemeProvider`), **RSS** (mark-read-on-scroll only), **Server** (URL + sign-in note); RSS list no longer duplicates that toggle; shared `getColors` on Settings, RSS, dock, launcher sheet, and article reader modal.
- Fix: **Editor / document patches** — frontmatter is never modified by `apply_operations_directly`, `insert_at` proposals, or resolver detection when the closing `---` has no trailing newline; orchestrator `frontmatter_end_index` accepts CRLF.
- Feature: **Local proxy** — per-device `capabilities_policy` in Settings (capabilities + path/shell limits), server push `set_policy` over WebSocket, shell approval **Run/Skip** in chat, grant/reject REST, Telegram/Discord `approve|skip <uuid>` quick replies, and `notify_shell_approval` notification cascade.
- Feature: **Bastion Mobile** — bottom **dock + app launcher**; **RSS** unified reader at `/(app)/rss` (`GET /api/rss/articles` cross-feed with **per-feed merge fallback** on 404 for older servers; **in-app reader** via `react-native-webview` and `GET /api/rss/articles/{article_id}`; source sheet with SecureStore last source, Unread/All/Starred, optional scroll-to-mark-read, mark-all-read for All or current feed; removed `rss/[feedId]`).
- Update: **Artifact bridge** — allowlist `GET /api/rss/articles` and `GET /api/rss/articles/{id}` (was documented for skills but missing from `artifactBridge.js`).
- Feature: **ToDos (web + mobile)** — `/api/todos` returns parsed `creation_timestamp`, structured `notes`, and prose-only `body_preview`; web ToDos list shows notes and “Added” line, formatted scheduled/deadline, and labels **ToDos** (sidebar + tab).
- Feature: **Bastion Mobile ToDos** — hierarchy matching web: collapsible **per-file** headers when multiple files, **section** rows with chevron for org heading parents, nested todo cards indented; **Created** date as a planning row (calendar icon) like Scheduled/Deadline; typed `parent_levels` on list items.
- Fix: **Bastion Mobile** — `VoiceFab` and `VoiceShortcutProvider` live under `mobile/src/voice/` (not `app/(app)/`) so Expo Router does not register them as tab routes; tab bar shows only the five real tabs again.
- Feature: **Smart Notification Router** — surface-aware in-app delivery (`WebSocketManager` registry), optional **Expo push** cascade, `notification_log` + `mobile_push_tokens`, REST `/api/notifications/*`, `X-Surface-Id` on orchestrator streams, desktop `TeamContext` + mobile surface WebSocket.
- Feature: **Bastion Mobile** — **Deep links** `bastion://shortcut-send?m=…` (text fire-and-forget to default chat) and `bastion://voice` (open voice modal); shared `quickSendToDefaultAgent`; `VoiceShortcutProvider` + hidden tab routes; documented in `mobile/BUILD.md`.
- Feature: **Bastion Mobile** — global **voice FAB** (all tabs): mic opens a modal, auto-starts recording, **Stop** transcribes and fire-and-forgets to `streamOrchestrator` (`session_id: bastion-mobile-voice`, new `Voice Note` conversation); Chat Send is text-only again. `SafeAreaProvider` at app root for FAB insets (requires `expo-av`).
- Feature: **BBS Data Workspace** — table browse (`render_table` + schema column order, terminal width fit, optional column truncation note) and per-row detail view after paging.
- Update: **Bastion Mobile ToDos** — tab label "ToDos"; compact file context and formatted scheduled/deadline; state changes use `/api/org/settings/todo-states` (tap default, long-press full list) with confirmation; org `update_todo` writes `CLOSED` for any user-configured done keyword.
- Fix: **BBS SSH** — show welcome art after protocol auth; start idle screen-blank watcher in `run_after_authenticated` so `BBS_SCREEN_BLANK_AFTER_SECONDS` applies to SSH and telnet.
- Feature: **Bastion Mobile** — Messages use server `display_name` and participant line; **Bastion Chat** tab (model picker via `/api/user/models/*`, long-press New chat for recent threads, document context banner + FAB from document reader); todos **org file** picker (long-press file chip) and **tag** filter row.
- Fix: **Mobile Android** — Android 15 / 16 KB native ELF alignment (`expo-build-properties` with NDK `27.1.12297006`; CI installs matching `ndk` package).
- Refactor: **Document service** — split phase-2 gRPC into focused mixins, lazy `FolderService` singleton on handler base, typed admin RPCs (`AdminGetDocument`, `FilterDocuments`, etc.) with shared `document_admin_ops`; backend facade and team cleanup call typed clients instead of `DocumentMirror` JSON actions.
- Feature: **Agent Factory playbooks** — optional per-step **`enabled: false`** skips the step at runtime (with `_skipped` / `_reason` in state) before evaluating **`condition`**; Workflow Composer toggle and validation updates.
- Fix: **Mobile Android debug APK** — embed JavaScript at Gradle build time (`debuggableVariants = []` via config plugin) so CI artifacts install without Metro; default Expo/RN debug skips bundling and expects port 8081. Plugin must not treat Expo’s **commented** `debuggableVariants` line as present, or injection is skipped and the APK still has no bundle.
- Fix: **Mobile Android CI** — add **`expo-asset`** (~11.0.5) so `export:embed` / `createBundleDebugJsAndAssets` can load `@expo/metro-config`; set **`NODE_ENV=production`** for the Gradle assemble step (Expo CLI requires it when bundling outside `expo start`).
- Feature: **Bastion Mobile** — first-run **Server** screen to enter and persist Bastion origin (SecureStore); login shows current URL and **Change server**; saved URL overrides build-time `EXPO_PUBLIC_API_BASE_URL` when set.
- Feature: **Bastion Mobile UX** — bottom **tab bar** (Todos, Docs, Messages, AI, Profile); nested stacks for document and room detail; **Profile** tab (server URL, change server, sign out); AI chat **Markdown** + scroll-to-end; room chat **self/other** bubbles and formatted times; **dayjs** relative times on room list; inline **error** banners on lists and **Alert** on todo toggle failure.
- Feature: **Bastion Mobile — Todos** — list matches **GET /api/todos** shape (`heading`, `todo_state`, `preview`, tags, planning lines); **All org files / Inbox** scope, debounced search, state chips, **Mark done / Reopen**; typed **`OrgTodoListItem`** in `mobile/src/api/todos.ts`.

## [0.70.8] - 2026-04-26

- Feature: **Bastion Mobile** — new `mobile/` Expo (React Native) app: Tier 1 (auth, todos, read-only documents) and Tier 2 (in-app messaging WebSockets, orchestrator streaming chat); GitHub Actions `build-mobile-android.yml` (debug APK) and manual `build-mobile-ios.yml` placeholder; mobile app version aligned with monorepo (`0.70.8`).
- Feature: **Settings — Local proxy (devices)** — live Online/Offline from server WebSocket state, optional live device ID when it differs from the label, refresh control and periodic refetch while the panel is open.
- Fix: **Device tokens** — `GET /api/settings/device-tokens` merges in-process WebSocket presence (`connected`, `live_device_id`); device proxy WebSocket registers `token_id` on `register_device` so presence matches tokens after hello/reconnect.
- Fix: **Mobile Android CI** — `permissions: contents: write` for GitHub Releases; `action-gh-release@v2` with explicit token.

## [0.70.7] - 2026-04-26

- Fix: **LLM agent tool results** — `llm_agent` steps only passed `formatted` to the model as `ToolMessage` content, so **`local_read_file`** (and similar tools) dropped **`content`**, **`entries`**, etc. Pipeline executor now appends JSON for remaining payload keys (with truncation via existing `MAX_TOOL_RESULT_CHARS`).
- Fix: **WebSockets behind reverse proxies** — frontend nginx uses dedicated `/api/ws/` and `/api/messaging/ws` locations (long timeouts, `Upgrade` only there); backend enables Uvicorn **`--ws-ping-interval` / `--ws-ping-timeout`** by default (`UVICORN_WS_PING_*`); **`docs/DEPLOYMENT.md`** §2.4 documents edge proxy requirements and **`UVICORN_WORKERS=1`** for the local proxy.
- Feature: **Local proxy** — headless mode (`--headless` / `cargo build --no-default-features`), optional `gui` Cargo feature, Debian `.deb` (`cargo deb`), systemd unit and postinst for `bastion-proxy` user; clearer errors for display-only capabilities on headless hosts.
- Fix: **Local proxy CI** — split Linux: desktop binary on `ubuntu-latest` (PipeWire/libspa matches xcap); headless `.deb` on `ubuntu-22.04` with `--no-default-features` (no `native-screenshot` / xcap) for Debian 12–compatible Depends. Optional Cargo feature **`native-screenshot`** (default on) pulls xcap; omit for bookworm-era libpipewire toolchains (nashaofu/xcap#247).

## [0.70.6] - 2026-04-23

- Update: **Settings** — remove unused **News** tab; merge **Wallpaper** into **Appearance**; legacy **`?tab=wallpaper`** / **`?tab=news`** redirect to **`appearance`** / **`rss-feeds`**.
- Fix: **document-service file watcher** — entertainment KG import pointed at non-existent `ds_services.entertainment_kg_extractor`; use **`shims.services.entertainment_kg_extractor`** (stub) so re-process logs are clean on greenfield.
- Fix: **`document_folders` RLS** — `UPDATE` policy required `user_id IS NOT NULL` for the admin branch, so **global** rows (`user_id` NULL) could not be updated; `INSERT ... ON CONFLICT DO UPDATE` on global root therefore failed on greenfield. Migration **`159_fix_document_folders_global_update_rls.sql`**; **`01_init.sql`**; **`run_migration.py`** alias **`159`** / **`document_folders_global_update_rls`**. Recreate the Postgres data volume or run **`159`** on existing DBs.
- Fix: **User document reprocess** — chunk-ineligible types (e.g. **`.org`**) are outside Qdrant primary indexing policy; the pipeline used to return success without running the v2 path, leaving the row stuck in **processing** after the API cleared vectors. Reprocess now resolves the file on disk, runs **metadata-only** processing via v2, and **force** re-queues on the parallel processor can replace a stuck in-flight slot.
- Fix: **POST `/api/settings/default-chat-agent-profile`** returned 404 (no matching profile) for valid custom agents: **`agent_profiles`** is RLS-protected but Settings handlers did not set `app.current_user_id`, so the visibility check saw zero rows. Pass **`rls_context`** in **`get_default_chat_agent_profile_detail`** and **`set_default_chat_agent_profile_id`**.
- Fix: **Default persona** when the user has not set **`default_persona_id`** is **Professional** (built-in UUID `b1b2c3d4-…000001`), not the first row by alphabetic name (which was **Abraham Lincoln**). **`get_default_persona`** in **`settings_service.py`**.
- Change: **Default Agent Playbook** (`...000001`) — `max_iterations` 20; step `prompt_template` includes `{query}`, optional `{history}` and `{editor}` (conditional blocks) for a better general-assistant default. Brownfield: migration **`158_default_playbook_prompt_and_iterations.sql`**; **`run_migration.py`** alias **`158`** / **`default_playbook_prompt`**.
- Change: **build-and-push** manual runs use a single **image** dropdown (all, postgres_only, or one service) instead of **build scope** + free-text **build targets**.
- Change: **Agent Factory** — greenfield and app startup only seed the **Bastion Assistant** built-in profile; **RSS Manager**, **DevOps Advisor**, and the **Deep research** / **RSS Manager** built-in playbooks are no longer pre-seeded. Brownfield: **`157_remove_rss_and_deep_research_builtin_playbooks.sql`**, included from **`09_greenfield_extensions.sql`**, removes prior rows. **`run_migration.py`** alias **`157`** / **`remove_rss_deep_research_playbooks`**.

## [0.70.5] - 2026-04-22

- Change: **Greenfield DB closure** — add **`09_greenfield_extensions.sql`** (`\ir` **039**, **048**, **054**, **071**, **083**, plus **`156_greenfield_agent_line_watches_workspace.sql`**) for objects previously missing from **`01_init.sql`** (conversation attachments, event watches, document edit proposals, browser sessions, chunk page columns + metadata FTS, **`agent_line_watches`** / **`agent_line_workspace`** with grants/RLS). **`01_init.sql`**: **`agent_lines.reference_config`** and **`data_workspace_config`**. Rename duplicate **`080_add_mcp_servers.sql`** → **`155_add_mcp_servers.sql`**; **`run_migration.py`** aliases **`155`** / **`156`**. Regenerate matrix: **`python backend/scripts/generate_greenfield_coverage.py`** → **`backend/postgres_init/GREENFIELD_COVERAGE.md`**. See [`docs/postgres-init.md`](docs/postgres-init.md).
- Fix: Greenfield main DB — add **`08_user_llm_providers.sql`** so **`user_llm_providers`** and **`user_enabled_models`** exist (migrations **055**, **080**); fixes Settings → Models and **`/api/user/llm-providers`** on fresh volumes. Existing volumes: run **`055`** then **`080`** via **`run_migration.py`** or recreate the Postgres data volume after pulling the image.
- Fix: **data-service** — on pool startup, if **`public.data_workspaces`** is missing (common when **`postgres-data`** reused a volume and Docker skipped **`docker-entrypoint-initdb.d`**), apply bundled **`sql/01_init.sql`** via **`psql`** (`postgresql-client` added to the data-service image). Complements the **`postgres-data`** init script for greenfield and volume-upgrade cases.
- Fix: Messaging WebSocket — register **`/api/messaging/ws/user`** before **`/api/messaging/ws/{room_id}`** so the user notification socket is not captured as **`room_id="user"`**. **`update_user_presence`** skips inserts when **`users`** has no matching **`user_id`** (avoids FK errors from stale JWTs after DB resets).
- Fix: **`agent_lines.handle`** (and partial unique index) in **`01_init.sql`** for greenfield — matches migration **093** after **101** rename so **`/api/agent-factory/handles`** queries succeed.
- Fix: **`postgres-data`** image runs **`00_init_data_workspace.sh`**, which applies **`data-service/sql/01_init.sql`** via **`psql -f`**, because nested files under **`docker-entrypoint-initdb.d/migrations/`** are not executed by the stock Postgres entrypoint (only top-level `*.sql` / `*.sh`). SQL is copied to **`/var/lib/bastion-data-workspace-sql/`**. Recreate the **`bastion_data_workspace_db`** volume after pulling this image so init runs.
- Fix: **`01_init.sql`** greenfield — add **`is_locked`** to **`agent_profiles`**, **`data_source_connectors`**, and **`external_connections`** (parity with migration **058**); **`ALTER ADD COLUMN IF NOT EXISTS`** for older init-created tables. Relax **`agent_skills`** RLS insert/update/delete so global built-in rows (**`user_id` NULL**, **`is_builtin`**) can be seeded and updated by **`bastion_user`**.
- Fix: Postgres init wrapper **`07_messaging_improvements.sql`** runs migration **`130_messaging_improvements`** on fresh volumes so **`users.is_bot`**, **`agent_profiles.bot_user_id`**, and **`chat_messages`** reply-to / edit columns exist (required by messaging and agent factory). See [`docs/postgres-init.md`](docs/postgres-init.md) greenfield closure. **`run_migration.py`** accepts **`--migration 130`** (and aliases **`messaging_improvements`** / **`130_messaging_improvements`**).
- Change: Postgres init wrapper **`06_learning_and_message_branching.sql`** runs migrations **`031_add_learning_progress`** and **`112_message_branching`** on fresh volumes (after **`05_federation_phases.sql`**); grants on **`learning_progress`** for **`bastion_user`**; **`031`** indexes use **`IF NOT EXISTS`**; **`112`** policies use **`DROP POLICY IF EXISTS`** for safe re-runs. See [`docs/postgres-init.md`](docs/postgres-init.md) greenfield closure.
- Fix: Greenfield **`agent_skills.required_connection_types`** (migration **126**) is now created in **`01_init.sql`** so [`agent_skills_service.py`](backend/services/agent_skills_service.py) inserts do not fail on fresh volumes.
- Change: **Greenfield DB consolidation** — `01_init.sql` now includes Agent Factory **skills** DDL (merged **068**, **131**, **134**, **135**, **136**, **133**), **`document_chunks.qdrant_point_id`** (**132**), **`kg_write_backlog` / `vector_embed_backlog`** (**152**–**153**), per-document **encryption** columns on **`document_metadata`** (**141**), **`user_home_dashboards`** (**111**), and **`oregon_trail_saves`** (**129**); new init wrapper **`05_federation_phases.sql`** runs **143**–**147** and **146**. Source migration files kept idempotent for brownfield; [`docs/postgres-init.md`](docs/postgres-init.md) documents **greenfield closure**. Oregon Trail API uses **`varchar`** user id in SQL to match **`users.user_id`**.
- Change: Postgres Docker init SQL lives under **`backend/postgres_init/`** only (image `COPY` whitelist); duplicate **`12_music_tables.sql`** removed; operator notes in [`docs/postgres-init.md`](docs/postgres-init.md) (init order, volume reset, migration bundling policy). **`run_migration.py`** loads files from `backend/postgres_init/migrations/`. Migration **108** partial indexes are created in **`01_init.sql`**; **`schema_guards`** verifies 108 schema only (no `CREATE INDEX` / `ALTER` as app role).
- Update: `01_init.sql` removes legacy GitHub org-sync DDL (with idempotent `DROP` for old volumes), seeds built-in `custom_playbooks` `...000001` / `...000003` (merged former migrations 076 + 113), adds `custom_playbooks.is_locked`; removed upgrade-only files `046`, `070`, `073`, `075`, `076`, `098`, `113`, `117`, `124`, `125` plus earlier `004`–`041` duplicates; `run_migration.py` checks for `messaging`, `039`, `076`, `098`; docs in [`docs/postgres-init.md`](docs/postgres-init.md).
- Fix: `backend/postgres_init/02_document_sharing.sql`, `03_document_chunk_index_state.sql`, and `04_document_processing_resilience.sql` run `\c bastion_knowledge_base` before `\ir` so Docker init applies migrations in the app DB (each init file is a new `psql` session).
- Feature: **`bastion-postgres`** and **`bastion-postgres-data`** images bundle `backend/postgres_init` and `data-service/sql` so deployments work without bind-mounting the repo; CI publishes them alongside other services. Compose uses `postgres/Dockerfile` and `postgres-data/Dockerfile` by default.
- Update: `build-and-push` manual runs support **build scope** `postgres_only` (DB images only) vs `all` (default); tag pushes always build all images.
- Update: `build-and-push` uses separate GHCR package families — **`bastion-<service>`** for production tags (e.g. `v0.70.0` from `main`) and **`bastion-dev-<service>`** for `-dev` versions (e.g. `v0.70.5-dev` from `dev`); workflow validates tag commits against `main`/`dev`. See `.github/workflows/README.md` for visibility (public vs private) on GitHub Container Registry.
- Update: In-app help — **Data Workspaces overview** and getting-started cross-links aligned with the current UI (databases, import formats, Run SQL, grid features, sharing, agent binding); help-docs sync manifest hashes refreshed.
- Fix: `build-local-proxy` Linux CI installs `libpipewire-0.3-dev` so `libspa-sys` (PipeWire) links during `cargo build`.
- Fix: `build-local-proxy` Linux CI installs `libgbm-dev` and `libxdo-dev` so the release link step finds `-lgbm` and `-lxdo` (egui/glutin and tray stack).
- Fix: `local-proxy` — export `CreateDirectoryCapability`, match `git2::BranchType` in `git_info`, and box recursive async helpers (`list_dir_recursive`, `build_tree`, `walk_and_search`) for stable Linux CI builds.
- Feature: `build-and-push` workflow can be run manually (`workflow_dispatch`); optional version input or `VERSION` file when not tagging.
- Fix: `build-and-push` workflow frees runner disk before Docker builds and uses GHA BuildKit cache `mode=min` to reduce disk use during multi-image tag pipelines.
- Update: BBS RSS article list remembers All vs Unread choice for the rest of the telnet session.
- Update: BBS RSS article viewer uses html2text with links and images omitted for cleaner terminal text (article `Link:` line unchanged).
- Feature: RSS article list All/Unread (BBS `[V]` toggle; web RSS viewer segment buttons); BBS wallpaper pane hides the footer bar and exits with Esc (plus q; arrow keys discarded as non-quit).
- Feature: BBS menu prompts use single-key `[letter]` shortcuts without Enter (multi-digit row numbers still read digits then Enter).
- Fix: Frontend Docker build uses Node 22 and `NODE_OPTIONS=--max-old-space-size=4096` so `vite build` meets dependency engine ranges and avoids heap OOM on CI.
- Update: `.env.example` lists every variable referenced in root `docker-compose.yml` (with compose-aligned defaults) plus application-only env vars for migration toward externalized / Kubernetes config.
- Update: `docker-compose.yml` documents `${VAR:-default}` pattern; DB URLs, JWT, Qdrant/Neo4j, LangGraph DB creds, data-workspace Postgres, prod frontend `VITE_*` runtime, and pgbouncer healthcheck honor `.env` overrides with same defaults as before when unset.
- Update: Docker Compose uses single `LOG_LEVEL` for all first-party services (removed per-service `*_LOG_LEVEL` env indirection).
- Fix: `build-and-push` workflow sets `IMAGE_PREFIX` to lowercase `github.repository_owner` so GHCR tags validate (e.g. `ghcr.io/bastion-workspace/bastion-backend:0.70.5`).
- Update: README — drop standalone **Deployment** section; fold operator link into **Technical Architecture** (see `docs/DEPLOYMENT.md`).
- Update: README — dedicated **Agent Factory** section (building blocks, hybrid skill discovery, concrete examples); **Agent Factory** snapshot + skill rows; remove premature **code-workspace** claims; reframe long-form editing as user-composed agents.
- Update: README product overview — platform snapshot, supported formats table, and expanded capability sections for operators and evaluators.
- Update: GitHub Actions `build-and-push` publishes production `frontend` only; `frontend-dev` (Vite HMR) is not built in CI—use `docker compose --profile dev --build` locally.
- Update: Added `docs/DEPLOYMENT.md` (compose scenarios, env grouping, GHCR, operations) and linked it from `README.md`.

## [0.70.0] - 2026-04-20

Release 0.70.0 aggregates a large set of improvements across the web app, BBS, document pipeline, Agent Factory / agent lines, Docker stacks, tooling, and operational hardening since 0.55.0. Prior per-change entries were retired in favor of this milestone cut; see git history for granular commits.

- Fix: GitHub Actions image builds use annotated tag `v0.70.0` (pattern `v*`), replacing non-matching tag `0.70.0`.

