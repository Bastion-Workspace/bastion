# Celery Flower - Monitoring UI Only
# Minimal dependencies for task monitoring. Codegen: grpcio-tools in requirements-build.txt (Docker build only).

# Core Celery
celery==5.5.3
flower==2.0.1
redis==6.4.0

# Cron expression parsing (scheduled_agent_tasks)
croniter==3.0.0

# gRPC runtime for generated *_pb2 / *_pb2_grpc
grpcio==1.80.0
protobuf>=6.31.1,<7

# Configuration (needed for config.py imports)
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.0.1

# HTTP client (needed for external_connections_service via connection_health_tasks)
httpx==0.28.1

# Database (needed for service_container imports)
psycopg2-binary==2.9.10
asyncpg==0.30.0
sqlalchemy==2.0.36

# Authentication (needed for service_container imports)
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2

# Token decryption (external_connections_service used by connection_health_tasks)
cryptography>=42.0.0
