-- Superseded: the RSS Manager built-in playbook and rss-manager profile are no longer auto-seeded.
-- Historical inserts were removed. For existing DBs, see 157_remove_rss_and_deep_research_builtin_playbooks.sql.
SELECT 1;
