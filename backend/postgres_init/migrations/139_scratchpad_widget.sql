-- 139: Scratch Pad home dashboard widget
-- Scratchpad data is stored in user_settings (key: scratchpad_pads, data_type: json).
-- No DDL change required; user_settings schema is sufficient.
-- Default pads are returned from GET /api/scratchpad until the user saves.

SELECT 1;
