# Celery worker runs the same ServiceContainer and document stack as the API.
# Installing the canonical backend lockfile prevents drift and `No module named '…'`
# failures. Pip resolves `-r` relative to this file's directory (both files live in /app).
-r requirements.txt
