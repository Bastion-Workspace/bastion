# FastAPI and ASGI server
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# gRPC server for tool service (Phase 2). Codegen: grpcio-tools in requirements-build.txt (Docker build only).
grpcio==1.80.0
grpcio-health-checking==1.80.0
# Required for generated *_pb2.py (grpcio does not install protobuf); align with grpcio-tools 1.80 protoc output
protobuf>=6.31.1,<7

# Celery for background processing
celery==5.5.3
flower==2.0.1

# LangChain ecosystem (0.3.x latest patches; see PyPI langchain / langchain-core)
langchain==0.3.28
langchain-openai==0.3.35
langchain-community==0.3.31
langchain-core==0.3.84
langsmith==0.4.13

# Embeddings (vector DB access is via vector-service gRPC only)
openai==1.109.1

# PostgreSQL database
psycopg2-binary==2.9.10
asyncpg==0.30.0
sqlalchemy==2.0.36

# Knowledge graph
neo4j==5.15.0
py2neo==2021.2.4

# Document processing
PyPDF2==3.0.1
pypdf==5.8.0
pdfplumber==0.11.4
PyMuPDF==1.24.14
reportlab==4.2.5
python-docx==1.1.2
python-pptx>=1.0.0
ebooklib==0.18
beautifulsoup4==4.12.3
ocrmypdf==17.2.0
email-reply-parser==0.5.12
zipfile36==0.1.3
pdf2image==1.17.0
pytesseract==0.3.13

# Text processing and NLP
nltk==3.9.1
textstat==0.7.4
langdetect==1.0.9

# Redis for caching
redis==6.4.0

# Per-document file encryption (Argon2id)
argon2-cffi==23.1.0

# Cron expression parsing for agent schedules
croniter==3.0.0

# HTTP client
httpx==0.28.1

# Model Context Protocol client (stdio, SSE, streamable HTTP)
mcp>=1.4.1
aiofiles==24.1.0

# Data processing
pandas==2.2.3
pyarrow>=15.0.0
numpy==1.26.4
scikit-learn==1.5.2

# Visualization
plotly>=5.18.0
kaleido>=0.2.1  # For static image export

# YAML serialization (agent bundle export/import)
pyyaml>=6.0

# Configuration and logging
pydantic==2.12.5  # ocrmypdf 17.x requires >=2.12.5; rest of stack compatible
pydantic-settings==2.13.1
python-dotenv==1.0.1
structlog==24.4.0
pytz==2024.1

# Authentication and security
PyNaCl>=1.5.0
python-jose[cryptography]==3.3.0
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
# Pin bcrypt to a passlib-compatible version (bcrypt 4.x breaks passlib's checks)
bcrypt==3.2.2

# WebSockets for real-time updates
websockets==13.1

# CRDT collaborative editing (Yjs protocol, YRoom WebSocket server)
pycrdt>=0.12.16,<0.13.0
pycrdt-websocket>=0.16.0

# Image processing (for OCR)
Pillow==11.0.0

# Progress tracking
tqdm==4.67.1

datasets==4.0.0
nest-asyncio==1.6.0

# Agent framework dependencies
feedparser==6.0.11
aiohttp>=3.11.11

# Universal content extraction
readability-lxml==0.8.1
newspaper3k==0.2.8
trafilatura==2.0.0

# LangGraph for agentic architecture (1.0.1 + prebuilt 1.0.1: last pair compatible with langchain-core 0.3.x; prebuilt>=1.0.2 requires langchain-core>=1.0)
langgraph==1.0.1
langgraph-prebuilt==1.0.1
langgraph-checkpoint-postgres>=3.0.5,<4.0.0
psycopg[binary]>=3.1.0  # Required for langgraph-checkpoint-postgres (bundled libpq on slim images)

# WebDAV server for OrgMode mobile sync
WsgiDAV==4.3.3
Cheroot==10.0.1
watchdog==4.0.0

# Org-Mode parsing
orgparse==0.4.20250520

# Graph processing for system modeling
networkx==3.3

# PDF export via HTML-to-PDF (WeasyPrint >=63 required for pydyf 0.11+ API; 62.x breaks with AttributeError on stream.transform)
weasyprint==65.1
markdown==3.7
