/** Visual height of the bottom dock bar (excluding safe-area inset). */
export const DOCK_CONTENT_HEIGHT = 56;

/** Mini player strip above the dock when audio is active. */
export const MINI_PLAYER_STRIP_HEIGHT = 56;
