# Copy to .env locally (not committed). Bastion API origin without trailing slash.
# Optional: the app can ask for the server URL on first launch and save it (see app (auth) server screen).
EXPO_PUBLIC_API_BASE_URL=https://your-bastion-host
